// Final-build guard: keep public money titles canonical across repeated local and audit builds.
const fs=require('fs');const path=require('path');const file=path.join(process.cwd(),'data','money-intelligence-registry.json');if(!fs.existsSync(file))process.exit(0);const data=JSON.parse(fs.readFileSync(file,'utf8'));for(const category of data.categories||[]){const current=String(category.originalTargetTitle||category.title||'').replace(/\s+—\s+\d+\s+reviewed routes\s+\(building to\s+\d+\)$/i,'').trim();category.originalTargetTitle=current;category.title=current;}fs.writeFileSync(file,JSON.stringify(data,null,2)+'\n');console.log(`Money intelligence category titles normalized: ${(data.categories||[]).length} canonical titles.`);
