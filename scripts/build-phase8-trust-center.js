const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'trust-center.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/trust-center.json found. Skipping Phase 8 Trust Center build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
function esc(s = '') { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
function nav() { return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="books.html">Books</a><a href="conversion-funnel.html">Funnels</a><a href="trust-center.html">Trust Center</a><a href="answer-engine.html">AI Answers</a><a href="power-atlas.html">Power Atlas</a><a href="network-map-index.html">Map Index</a><a href="evidence-vault.html">Evidence Vault</a><a href="news.html">Intel Desk</a><a href="forum.html">Signal Board</a><a href="search.html">Search</a><a href="black-file.html">Black File</a></nav></header>`; }
function layout(title, desc, body) { return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Trust Center: records before claims, association is not guilt, correction is strength.</p></footer></div><script src="matrix.js"></script></body></html>`; }
function cards() { return (data.pages || []).map(p => `<article class="card redline"><span class="label">${esc(p.label)}</span><h3>${esc(p.title)}</h3><p>${esc(p.summary)}</p><a class="btn" href="trust-${esc(p.slug)}.html">Open Policy</a></article>`).join(''); }
function pageBody(p) { return `<main><section class="hero wrap"><div class="eyebrow">Phase 8 Trust Center</div><h1>${esc(p.title)}</h1><p class="lead">${esc(p.summary)}</p><div class="cta-row"><a class="btn" href="trust-center.html">Trust Center</a><a class="btn alt" href="evidence-policy.html">Evidence Policy</a><a class="btn alt" href="evidence-vault.html">Evidence Vault</a><a class="btn alt" href="conversion-funnel.html">Funnels</a></div></section><section class="section wrap split"><div class="terminal">TRUST DISCIPLINE\n&gt; Records before claims\n&gt; Association is not guilt\n&gt; Symbolism is commentary unless sourced otherwise\n&gt; Corrections strengthen credibility\n&gt; Forms disclose reader-path collection</div><aside class="card redline"><h2>${esc(p.label)}</h2><p>${esc(p.summary)}</p></aside></section><section class="section wrap"><h2>Standards</h2><div class="grid">${(p.sections || []).map(s => `<article class="card"><span class="label">Rule</span><h3>${esc(s)}</h3><p>This keeps the archive dark, useful, and evidence-bound.</p></article>`).join('')}</div></section></main>`; }
const generated = [];
for (const p of data.pages || []) {
  const file = `trust-${p.slug}.html`;
  fs.writeFileSync(path.join(root, file), layout(`${p.title} | Matrix Reprogrammed`, p.summary, pageBody(p)));
  generated.push(file);
}
const principleCards = (data.principles || []).map(p => `<article class="card"><span class="label">Principle</span><h3>${esc(p)}</h3><p>Applied across funnels, books, maps, answer pages, evidence lanes, and source cards.</p></article>`).join('');
const indexBody = `<main><section class="hero wrap"><div class="eyebrow">Phase 8 Trust Center</div><h1>TRUST CENTER.</h1><p class="lead">The public-record archive is only powerful when the claim labels are clean. This center explains source methodology, corrections, privacy, association boundaries, and editorial standards.</p><div class="cta-row"><a class="btn" href="trust-privacy-policy.html">Privacy Policy</a><a class="btn alt" href="trust-source-methodology.html">Source Methodology</a><a class="btn alt" href="trust-corrections-policy.html">Corrections</a></div></section><section class="section wrap split"><div class="terminal">TRUST ENGINE STATUS\n&gt; Policy pages: ${(data.pages || []).length}\n&gt; Principles: ${(data.principles || []).length}\n&gt; Funnel privacy route: active\n&gt; Search index: expanded\n&gt; Sitemap: expanded\n&gt; llms.txt: expanded</div><aside class="card redline"><h2>Credibility Shield</h2><p>Dark research only works when the boundary between record, allegation, association, and interpretation stays visible.</p></aside></section><section class="section wrap"><h2>Trust Pages</h2><div class="grid">${cards()}</div></section><section class="section wrap"><h2>Operating Principles</h2><div class="grid">${principleCards}</div></section></main>`;
fs.writeFileSync(path.join(root, 'trust-center.html'), layout('Trust Center | Matrix Reprogrammed', 'Source methodology, privacy, corrections, claim labels, and editorial standards for Matrix Reprogrammed.', indexBody));
generated.push('trust-center.html');
function patchFile(file, marker, section) { const p = path.join(root, file); if (!fs.existsSync(p)) return; let html = fs.readFileSync(p, 'utf8'); if (html.includes(marker)) return; html = html.replace('</main>', `${section}</main>`); fs.writeFileSync(p, html); }
const trustSection = `<section id="phase-eight-trust-engine" class="section wrap"><h2>Phase 8 Trust Center</h2><p class="lead">Before entering the archive, read the rules: source methodology, association boundary, corrections, and privacy.</p><div class="cta-row"><a class="btn" href="trust-center.html">Trust Center</a><a class="btn alt" href="trust-privacy-policy.html">Privacy Policy</a><a class="btn alt" href="trust-association-boundary.html">Association Boundary</a></div></section>`;
patchFile('conversion-funnel.html', 'id="phase-eight-trust-engine"', trustSection);
patchFile('black-file.html', 'id="phase-eight-trust-engine"', trustSection);
function addPagesToSitemap(files) { const p = path.join(root, 'sitemap.xml'); if (!fs.existsSync(p)) return; let xml = fs.readFileSync(p, 'utf8'); const add = files.filter(f => !xml.includes(`/${f}</loc>`)).map(f => `  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated || '2026-06-24')}</lastmod><changefreq>weekly</changefreq><priority>0.91</priority></url>`).join('\n'); if (add) xml = xml.replace('</urlset>', `${add}\n</urlset>`); fs.writeFileSync(p, xml); }
function patchLlms(files) { const p = path.join(root, 'llms.txt'); if (!fs.existsSync(p)) return; let llms = fs.readFileSync(p, 'utf8'); if (llms.includes('/trust-center.html')) return; fs.writeFileSync(p, `${llms.trim()}\n\nPhase 8 Trust Center:\n- /trust-center.html: editorial trust hub for source methodology, privacy, corrections, claim labels, and association boundaries.\n${files.filter(f => f !== 'trust-center.html').map(f => `- /${f}: generated trust policy page.`).join('\n')}\n`); }
function patchSearchIndex(files) { const p = path.join(root, 'search-index.json'); if (!fs.existsSync(p)) return; const search = JSON.parse(fs.readFileSync(p, 'utf8')); const existing = new Set(search.map(i => i.url)); for (const page of data.pages || []) { const url = `trust-${page.slug}.html`; if (!existing.has(url)) { search.push({ key: `trust-${page.slug}`, title: `${page.title} | Trust Center`, subtitle: page.label, series: 'Trust Center', category: 'Editorial Policy', url, description: page.summary, keywords: [page.title, page.label, page.summary, ...(page.sections || [])] }); existing.add(url); } } if (!existing.has('trust-center.html')) search.push({ key: 'trust-center', title: 'Trust Center | Matrix Reprogrammed', subtitle: 'Phase 8 Editorial Integrity', series: 'Trust Center', category: 'Editorial Policy', url: 'trust-center.html', description: 'Source methodology, privacy, corrections, claim labels, and editorial standards for Matrix Reprogrammed.', keywords: ['trust center','privacy','corrections','source methodology','claim labels','association boundary'] }); fs.writeFileSync(p, JSON.stringify(search, null, 2)); }
addPagesToSitemap(generated);
patchLlms(generated);
patchSearchIndex(generated);
console.log(`Built Phase 8 Trust Center with ${(data.pages || []).length} policy pages and ${generated.length} generated pages.`);
