const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'offer-stack.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/offer-stack.json found. Skipping Phase 18 Offer Stack build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
const outDir = path.join(root, 'downloads');
if (!fs.existsSync(outDir)) fs.mkdirSync(outDir, { recursive: true });
function esc(s = '') { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
function nav() { return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="offer-center.html">Offer Center</a><a href="launch-room.html">Launch Room</a><a href="share-center.html">Share Center</a><a href="download-center.html">Download Center</a><a href="sales-ladder.html">Reader Paths</a><a href="books.html">Books</a><a href="amazon-store-books.html">Amazon Store</a><a href="forum.html">Signal Board</a></nav></header>`; }
function layout(title, desc, body) { return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /><script type="application/ld+json">${JSON.stringify({'@context':'https://schema.org','@type':'OfferCatalog',name:title,description:desc})}</script></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Offer boundary: sales pages guide reading routes; they do not upgrade claim strength.</p></footer></div><script src="matrix.js"></script></body></html>`; }
function titleFromFile(file) { return file.replace(/^downloads\//, '').replace(/\.html$|\.json$|\.md$/g, '').replace(/-/g, ' '); }
function buttons(routes = []) { return routes.map(route => `<a class="btn alt" href="${esc(route)}">${esc(titleFromFile(route))}</a>`).join(''); }
function cards(items = []) { return items.map(item => `<article class="card"><span class="label">Offer Step</span><h3>${esc(item)}</h3><p>Use this step to move the reader toward the next route without losing the trust boundary.</p></article>`).join(''); }
function offerJson(o) { return { updated: data.updated, title: o.title, label: o.label, summary: o.summary, boundary: 'Offer pages are sales orientation assets. They do not increase evidence strength or claim certainty.', primaryRoute: o.primaryRoute, buyRoute: o.buyRoute, bookRoute: o.bookRoute, readerPath: o.readerPath, trustRoute: o.trustRoute, evidenceRoute: o.evidenceRoute, downloadRoute: o.downloadRoute, campaignRoute: o.campaignRoute, shareRoute: o.shareRoute, promise: o.promise, steps: o.steps }; }
function offerMarkdown(o) { return `# ${o.title}\n\n${o.summary}\n\n## Boundary\n\nOffer pages are sales orientation assets. They do not increase evidence strength or claim certainty.\n\n## Promise\n\n${o.promise}\n\n## Routes\n\n- Primary: ${o.primaryRoute}\n- Buy route: ${o.buyRoute}\n- Book route: ${o.bookRoute}\n- Reader path: ${o.readerPath}\n- Trust route: ${o.trustRoute}\n- Evidence route: ${o.evidenceRoute}\n- Download route: ${o.downloadRoute}\n- Campaign route: ${o.campaignRoute}\n- Share route: ${o.shareRoute}\n\n## Offer Steps\n\n${(o.steps || []).map(x => `- ${x}`).join('\n')}\n`; }
function buildOffer(o) {
  const htmlFile = `offer-${o.slug}.html`;
  const jsonFile = `downloads/offer-${o.slug}.json`;
  const mdFile = `downloads/offer-${o.slug}.md`;
  fs.writeFileSync(path.join(root, jsonFile), JSON.stringify(offerJson(o), null, 2));
  fs.writeFileSync(path.join(root, mdFile), offerMarkdown(o));
  const routeList = [o.primaryRoute, o.buyRoute, o.bookRoute, o.readerPath, o.trustRoute, o.evidenceRoute, o.downloadRoute, o.campaignRoute, o.shareRoute];
  const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 18 Offer Stack</div><h1>${esc(o.title)}</h1><p class="lead">${esc(o.summary)}</p><div class="cta-row"><a class="btn" href="${esc(o.buyRoute)}">Buy / Store Route</a><a class="btn alt" href="${esc(jsonFile)}">JSON Offer</a><a class="btn alt" href="${esc(mdFile)}">Markdown Offer</a><a class="btn alt" href="offer-center.html">Offer Center</a></div></section><section class="section wrap split"><div class="terminal">OFFER ROOM\n&gt; Offer: ${esc(o.title)}\n&gt; Label: ${esc(o.label)}\n&gt; Promise: ${esc(o.promise)}\n&gt; Buy route: ${esc(o.buyRoute)}\n&gt; Trust route: ${esc(o.trustRoute)}\n&gt; Evidence route: ${esc(o.evidenceRoute)}</div><aside class="card redline"><h2>Offer Boundary</h2><p>Sell the route. Keep claim strength inside the trust and evidence pages.</p></aside></section><section class="section wrap"><h2>Offer Routes</h2><div class="card"><div class="cta-row small">${buttons(routeList)}</div></div></section><section class="section wrap"><h2>Offer Steps</h2><div class="grid">${cards(o.steps)}</div></section><section class="section wrap"><h2>Downloads</h2><div class="card"><div class="cta-row"><a class="btn" href="${esc(jsonFile)}">JSON Offer</a><a class="btn alt" href="${esc(mdFile)}">Markdown Offer</a></div></div></section></main>`;
  fs.writeFileSync(path.join(root, htmlFile), layout(`${o.title} | Matrix Reprogrammed`, o.summary, body));
  return [htmlFile, jsonFile, mdFile];
}
function patchFile(file, marker, section) { const p = path.join(root, file); if (!fs.existsSync(p)) return; let html = fs.readFileSync(p, 'utf8'); if (html.includes(marker)) return; html = html.replace('</main>', `${section}</main>`); fs.writeFileSync(p, html); }
function addPagesToSitemap(files) { const p = path.join(root, 'sitemap.xml'); if (!fs.existsSync(p)) return; let xml = fs.readFileSync(p, 'utf8'); const add = files.filter(f => f.endsWith('.html') && !xml.includes(`/${f}</loc>`)).map(f => `  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated)}</lastmod><changefreq>weekly</changefreq><priority>0.89</priority></url>`).join('\n'); if (add) xml = xml.replace('</urlset>', `${add}\n</urlset>`); fs.writeFileSync(p, xml); }
function patchLlms(files) { const p = path.join(root, 'llms.txt'); if (!fs.existsSync(p)) return; let llms = fs.readFileSync(p, 'utf8'); if (llms.includes('/offer-center.html')) return; fs.writeFileSync(p, `${llms.trim()}\n\nPhase 18 Offer Stack / Revenue Ladder Engine:\n- /offer-center.html: public offer hub connecting books, Amazon store routes, reader paths, campaigns, share kits, downloads, trust pages, and evidence lanes.\n${files.map(f => `- /${f}: generated offer route or downloadable offer plan.`).join('\n')}\n`); }
function patchSearchIndex(files) { const p = path.join(root, 'search-index.json'); if (!fs.existsSync(p)) return; const search = JSON.parse(fs.readFileSync(p, 'utf8')); const existing = new Set(search.map(i => i.url)); for (const o of data.offers || []) { const url = `offer-${o.slug}.html`; if (!existing.has(url)) { search.push({ key: `offer-${o.slug}`, title: `${o.title} | Offer`, subtitle: o.label, series: 'Offer Stack Engine', category: 'Revenue Ladder', url, description: o.summary, keywords: [o.title, o.label, o.summary, o.primaryRoute, o.buyRoute, o.bookRoute, o.readerPath] }); existing.add(url); } } if (!existing.has('offer-center.html')) search.push({ key: 'offer-center', title: 'Offer Center | Matrix Reprogrammed', subtitle: 'Phase 18 Revenue Ladder Engine', series: 'Offer Stack Engine', category: 'Revenue Ladder', url: 'offer-center.html', description: 'Offer stack hub for Matrix Reprogrammed book-buying routes.', keywords: ['offer center','revenue ladder','book sales','amazon store','reader path','buy books'] }); fs.writeFileSync(p, JSON.stringify(search, null, 2)); }
const outputs = [];
for (const o of data.offers || []) outputs.push(...buildOffer(o));
const offerCards = (data.offers || []).map(o => `<article class="card redline"><span class="label">${esc(o.label)}</span><h3>${esc(o.title)}</h3><p>${esc(o.summary)}</p><a class="btn" href="offer-${esc(o.slug)}.html">Open Offer</a></article>`).join('');
const ruleCards = (data.rules || []).map(rule => `<article class="card"><span class="label">Offer Rule</span><h3>${esc(rule)}</h3><p>Offer routes must preserve trust and evidence boundaries while guiding the sale.</p></article>`).join('');
const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 18 Offer Stack / Revenue Ladder Engine</div><h1>OFFER CENTER.</h1><p class="lead">Book-buying routes that connect the Amazon store, reader paths, campaigns, share kits, download packs, trust pages, and evidence lanes.</p><div class="cta-row"><a class="btn" href="offer-starter-library.html">Starter Offer</a><a class="btn alt" href="offer-dog-architect-collector.html">D.O.G Offer</a><a class="btn alt" href="offer-full-archive.html">Full Archive</a></div></section><section class="section wrap split"><div class="terminal">OFFER STACK STATUS\n&gt; Offers: ${(data.offers || []).length}\n&gt; Rules: ${(data.rules || []).length}\n&gt; JSON offers: ${(data.offers || []).length}\n&gt; Markdown offers: ${(data.offers || []).length}\n&gt; Amazon route: active\n&gt; Trust route: active\n&gt; Evidence route: active</div><aside class="card redline"><h2>Revenue Boundary</h2><p>Offer pages sell the route into the archive. They do not turn interpretation into proof.</p></aside></section><section class="section wrap"><h2>Offer Routes</h2><div class="grid">${offerCards}</div></section><section class="section wrap"><h2>Offer Rules</h2><div class="grid">${ruleCards}</div></section></main>`;
fs.writeFileSync(path.join(root, 'offer-center.html'), layout('Offer Center | Matrix Reprogrammed', 'Offer stack hub for Matrix Reprogrammed book-buying routes.', body));
outputs.push('offer-center.html');
const section = `<section id="phase-eighteen-offer-stack-engine" class="section wrap"><h2>Phase 18 Offer Stack / Revenue Ladder Engine</h2><p class="lead">This route now connects to clear book-buying offers, Amazon routes, reader paths, campaign rooms, share kits, and trust boundaries.</p><div class="cta-row"><a class="btn" href="offer-center.html">Offer Center</a><a class="btn alt" href="offer-starter-library.html">Starter Offer</a><a class="btn alt" href="offer-full-archive.html">Full Archive Offer</a></div></section>`;
for (const file of ['index.html','books.html','amazon-store-books.html','sales-ladder.html','book-universe.html','launch-room.html','share-center.html']) patchFile(file, 'id="phase-eighteen-offer-stack-engine"', section);
addPagesToSitemap(outputs);
patchLlms(outputs);
patchSearchIndex(outputs);
console.log(`Built Phase 18 Offer Stack with ${(data.offers || []).length} offers and ${outputs.length} outputs.`);
