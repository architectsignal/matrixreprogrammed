const fs = require('fs');
const path = require('path');
const root = process.cwd();
const bulletins = JSON.parse(fs.readFileSync(path.join(root, 'data', 'bulletins.json'), 'utf8'));
const humanCost = JSON.parse(fs.readFileSync(path.join(root, 'data', 'human-cost.json'), 'utf8'));
const migrationPanel = JSON.parse(fs.readFileSync(path.join(root, 'data', 'migration-flow-panel.json'), 'utf8'));

function esc(s = '') {
  return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}
function nav() {
  return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="books.html">Books</a><a href="epstein-files.html">Epstein Files</a><a href="black-file-index.html">Index</a><a href="answer-index.html">AI Answers</a><a href="news.html">Intel Desk</a><a href="intel-archive.html">Archive</a><a href="timers.html">Timers</a><a href="black-file.html">Black File</a></nav></header>`;
}
function layout(title, desc, body) {
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /><link rel="stylesheet" href="fixes.css" /></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Public-record investigation, symbolic analysis, esoteric commentary, fiction, speculation, and author interpretation are separated where needed. Human-cost figures are category-specific and source-dated.</p></footer></div><script src="matrix.js"></script></body></html>`;
}
function sourceList(sources = []) {
  return sources.map(s => `<a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.label)}</a>`).join(' · ');
}
function bulletinCard(b) {
  return `<article class="news-item redline"><span class="label">${esc(b.label)}</span><h3>${esc(b.headline)}</h3><p>${esc(b.summary)}</p><p><strong>Why it matters:</strong> ${esc(b.why)}</p><p class="source-list">${sourceList(b.sources)}</p><div class="cta-row small"><a class="btn" href="${esc(b.path)}">${esc(b.pathLabel || 'Open Path')}</a></div></article>`;
}
function metricCard(p) {
  return `<div class="metric ${p.key.includes('conflict') || p.key.includes('migration') ? 'redline' : ''}"><strong>${esc(p.figure)}</strong><span><b>${esc(p.title)}</b><br>${esc(p.description)}<em>${esc(p.sourceLabel)}</em></span></div>`;
}
function humanCard(p) {
  return `<article class="card ${p.key.includes('conflict') || p.key.includes('migration') ? 'redline' : ''}"><span class="figure-caption">Worldwide / latest sourced figure</span><strong class="figure-block">${esc(p.figure)}</strong><h3>${esc(p.title)}</h3><p>${esc(p.description)}</p><p><span class="pill">${esc(p.status)}</span> <span class="pill">${esc(p.sourceLabel)}</span></p></article>`;
}
function migrationFigureCard(panel, index) {
  const labels = ['Encounters do not equal people', 'Detections do not equal people', 'Minimum reported count', 'Category boundary'];
  const className = index === 0 ? 'metric redline' : index === 1 ? 'metric amber' : 'metric';
  const track = Array.isArray(panel.track) ? panel.track.join('; ') : '';
  return `<div class="${className}"><strong>${esc(panel.publicFigure)}</strong><span><b>${esc(panel.region)}</b><br>${esc(panel.categoryWarning)}<br><small><strong>Track:</strong> ${esc(track)}</small><em>${esc(panel.sourceLabel)} · checked ${esc(panel.checked || migrationPanel.updated)}</em></span></div>`;
}
function sourceLink(url, label) {
  return `<a href="${esc(url)}" target="_blank" rel="noopener">${esc(label)}</a>`;
}
function countryStatusCard(country) {
  const statusClass = /NATIONALITY|SUSPECT/i.test(country.sourceSplitStatus || '') ? 'card amber' : 'card redline';
  return `<article class="${statusClass}"><span class="label">${esc(country.publicFigure)}</span><h3>${esc(country.country)}</h3><p><strong>Source split:</strong> ${esc(country.sourceSplitStatus)}</p><p>${esc(country.latestSourceStatus)}</p><p><strong>Available:</strong> ${esc((country.availableSplits || []).join('; '))}.</p><p><strong>Missing:</strong> ${esc((country.missingSplits || []).join('; '))}.</p><p><strong>Evidence warning:</strong> ${esc(country.evidenceWarning)}</p><p class="source-list">${sourceLink(country.sourceUrl, country.sourceLabel)}</p></article>`;
}
function migrationSection() {
  const countries = migrationPanel.sexualAssaultCountryLane.countries || [];
  return `<section id="migration" class="section wrap"><h2>${esc(migrationPanel.title)}</h2><p class="lead">${esc(migrationPanel.summary)} ${esc(migrationPanel.evidenceRule)}</p><div class="metric-grid">${migrationPanel.flowPanels.map(migrationFigureCard).join('')}</div><div class="card redline"><h3>Migration Display Rule</h3><p><strong>Do not write:</strong> “X million illegal immigrants entered” unless a reliable source specifically estimates that exact category.</p><p><strong>Use instead:</strong> “border encounters,” “irregular crossings,” “asylum applications,” “returns/removals,” “estimated unauthorized population,” “departures,” or “net migration,” depending on the dataset.</p><p class="source-list">${migrationPanel.flowPanels.map(panel => sourceLink(panel.sourceUrl, panel.sourceLabel)).join(' · ')} · ${sourceLink('migration-flow.html', 'Open full migration/source-split panel')}</p></div><section class="section"><h2>${esc(migrationPanel.sexualAssaultCountryLane.title)}</h2><p class="lead">${esc(migrationPanel.sexualAssaultCountryLane.summary)}</p><article class="card redline"><span class="label">EU official total marker</span><h3>Eurostat 2024 sexual-violence marker</h3><p class="figure-block">${esc(migrationPanel.sexualAssaultCountryLane.euHeadline)}</p><p>${esc(migrationPanel.sexualAssaultCountryLane.evidenceRule)}</p></article><div class="metric-grid"><div class="metric redline"><strong>256,302 sexual violence offences</strong><span><b>European Union 2024 marker</b><br>Police-recorded offences; not an immigration-status split.<em>Eurostat marker in site data</em></span></div><div class="metric amber"><strong>98,190 rape offences</strong><span><b>European Union 2024 marker</b><br>Police-recorded rape offences; not suspect, conviction, or migrant-status data.<em>Eurostat marker in site data</em></span></div><div class="metric"><strong>10 countries mapped</strong><span><b>Country source lanes</b><br>Official sexual-offence stats exist for every listed country; clean immigration-status split is the missing part.<em>ONS / SSMSI / BKA / Brå / ISTAT / INE / CBS / Statbel / BMI / FBI-BJS</em></span></div></div><div class="grid">${countries.map(countryStatusCard).join('')}</div></section></section>`;
}

const live = bulletins.bulletins.slice(0, 7);
const metrics = humanCost.panels.map(metricCard).join('');
const humanCards = humanCost.panels.map(humanCard).join('');
const newsBody = `<main><section class="hero wrap"><div class="eyebrow">Signal Intel Desk</div><h1>SHORT BULLETINS.<br>PUBLIC RECORD.</h1><p class="lead">A Matrix Reprogrammed source desk for wars, conflict deaths, migration and irregular immigration flows, Epstein file drops, intelligence agencies, declassified files, sanctions, court records, vaccine injury compensation, trafficking and exploitation networks, crime-state overlap, surveillance, censorship, and corruption.</p><div class="cta-row"><a class="btn" href="#latest">Latest 7 Days</a><a class="btn alt" href="intel-archive.html">Archive</a><a class="btn alt" href="#human-cost">Human Cost</a><a class="btn alt" href="#conflict-zones">Conflict Zones</a><a class="btn alt" href="#migration">Migration</a><a class="btn alt" href="#vaccines">Vaccines</a></div></section><section id="latest" class="section wrap split"><div><p class="dateline">Latest 7 Days · Updated ${esc(bulletins.updated)}</p>${live.map(bulletinCard).join('')}</div><aside class="card redline"><h2>Evidence Rule</h2><p>No unsupported accusations. No private victim names. No screenshots as proof. No rumor framed as fact. Every item must either link to a source or be marked clearly as not verified.</p><div class="terminal">CONFIRMED = official / court / verified source\nDEVELOPING = credible reporting, still moving\nESTIMATE = modelled or NGO estimate\nREPORTED = claim logged, not necessarily verified\nCOMPENSATED = claim accepted by scheme or court\nMIGRATION ENCOUNTER = border/contact statistic, not unique persons\nASYLUM CLAIM = application statistic, not illegal-entry count\nNOT VERIFIED = claim seen, not supported\nCAUSATION WARNING = report does not prove cause</div></aside></section><section id="human-cost" class="section wrap"><h2>Human Cost Evidence Panels</h2><p class="lead">War, migration, exploitation, medicine, policy, and criminal networks have human cost. These panels use sourced, dated figures only. They do not display fake live counters or unverifiable global totals.</p><div class="metric-grid">${metrics}</div><div class="grid">${humanCards}</div></section><section id="conflict-zones" class="section wrap"><h2>Conflict Zones · Worldwide Snapshot</h2><p class="lead">Conflict figures must be tracked by theater. There is no single honest global death counter that merges every war without destroying source meaning.</p><div class="metric-grid">${humanCost.panels.filter(p => p.key.includes('conflict') || p.key.includes('displacement') || p.key.includes('food') || p.key.includes('medical')).map(metricCard).join('')}</div></section>${migrationSection()}<section id="vaccines" class="section wrap"><h2>Vaccines · Deaths, Damages & Evidence Boundary</h2><p class="lead">This panel separates adverse-event reports, compensated injuries/deaths, pending claims, and proven causation. A report after vaccination is not automatically a death caused by vaccination.</p><div class="metric-grid"><div class="metric redline"><strong>VAERS = signal</strong><span>VAERS accepts reports after vaccination; raw reports alone cannot prove causation.<em>VAERS disclaimer</em></span></div><div class="metric amber"><strong>$4.6B+ paid</strong><span>Historic VICP compensation by May 2023. Use HRSA reports for current totals.<em>HRSA VICP</em></span></div><div class="metric"><strong>Separate scheme</strong><span>COVID countermeasure claims are handled separately from VICP. Use HRSA CICP reports for paid and pending totals.<em>HRSA CICP</em></span></div><div class="metric"><strong>High threshold</strong><span>UK Vaccine Damage Payment Scheme uses a high disability threshold; use official GOV.UK/FOI sources for current figures.<em>UK VDPS</em></span></div></div><p class="source-list"><a href="https://vaers.hhs.gov/data.html" target="_blank" rel="noopener">VAERS Data / Disclaimer</a> · <a href="https://www.hrsa.gov/vaccine-compensation" target="_blank" rel="noopener">HRSA VICP</a> · <a href="https://www.hrsa.gov/cicp" target="_blank" rel="noopener">HRSA CICP</a> · <a href="https://www.gov.uk/vaccine-damage-payment" target="_blank" rel="noopener">UK Vaccine Damage Payment Scheme</a></p></section><section id="sources" class="section wrap"><h2>Source Hubs</h2><div class="grid"><article class="card"><h3>Intelligence & Declassification</h3><p>CIA Reading Room, FBI Vault, NARA, ODNI, congressional releases, court dockets, and agency document libraries.</p></article><article class="card"><h3>Migration & Borders</h3><p>CBP, Frontex, Eurostat, IOM, UNHCR, national border agencies, asylum systems, removals, returns, departures, and irregular migration estimates.</p></article><article class="card"><h3>Trafficking & Exploitation</h3><p>Only law-enforcement, court, official, or carefully sourced public-interest records. No victim names, no graphic detail, no rumor.</p></article></div></section></main>`;
fs.writeFileSync(path.join(root, 'news.html'), layout('Signal Intel Desk | Matrix Reprogrammed', 'Matrix Reprogrammed Signal Intel Desk: sourced bulletins and human-cost evidence panels for wars, migration, irregular immigration, intelligence, Epstein files, vaccine compensation, trafficking, and institutional failure.', newsBody));
const archiveBody = `<main><section class="hero wrap"><div class="eyebrow">Intel Archive</div><h1>BULLETIN ARCHIVE.</h1><p class="lead">All Matrix Reprogrammed source-led bulletins move here after publication. Live news never disappears; it becomes archive structure.</p><div class="cta-row"><a class="btn" href="news.html">Live Intel Desk</a><a class="btn alt" href="epstein-files.html">Epstein Hub</a><a class="btn alt" href="dashboard-human-cost.html">Human Cost Dashboard</a></div></section><section class="section wrap"><div class="grid">${bulletins.bulletins.map(b => `<article class="card"><span class="label">${esc(b.label)} · ${esc(b.date)}</span><h3>${esc(b.headline)}</h3><p>${esc(b.summary)}</p><p><strong>Why it matters:</strong> ${esc(b.why)}</p><p class="source-list">${sourceList(b.sources)}</p><a class="btn" href="${esc(b.path)}">${esc(b.pathLabel || 'Open Path')}</a></article>`).join('')}</div></section></main>`;
fs.writeFileSync(path.join(root, 'intel-archive.html'), layout('Intel Archive | Matrix Reprogrammed', 'Archive of Matrix Reprogrammed source-led bulletins, Epstein drops, human-cost updates, migration panels, war files, intelligence, crime-state overlap, and public-record signals.', archiveBody));
console.log(`Built Intel Desk with ${live.length} live bulletins, ${migrationPanel.flowPanels.length} migration figure panels, ${migrationPanel.sexualAssaultCountryLane.countries.length} source-split country cards, and archive with ${bulletins.bulletins.length} total bulletins.`);
