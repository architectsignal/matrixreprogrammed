<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Membership | Matrix Reprogrammed</title>
  <meta name="description" content="Create a free Matrix Reprogrammed member account and prepare for premium intelligence access."/>
  <link rel="stylesheet" href="styles.css"/>
  <link rel="stylesheet" href="reader-experience.css"/>
  <style>
    .money-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:1rem}
    .money-card{border:1px solid rgba(216,181,106,.28);border-radius:22px;padding:1rem;background:linear-gradient(150deg,rgba(14,8,2,.96),rgba(0,0,0,.95))}
    .price{font-size:2rem;color:#d8b56a;font-weight:900}.mini{font-size:.84rem;color:#c8b98c}
    .member-form{display:grid;gap:.8rem;max-width:720px}.member-form input{width:100%;box-sizing:border-box;background:#050505;color:#f3e6bd;border:1px solid rgba(216,181,106,.35);border-radius:12px;padding:.85rem}
    .consent-row{display:flex;gap:.65rem;align-items:flex-start}.consent-row input{width:auto;margin-top:.25rem}.status{min-height:1.4rem;font-weight:700}.status.ok{color:#9ddd9d}.status.error{color:#ff9b8f}
    .account-actions{display:flex;gap:.7rem;flex-wrap:wrap}.disabled-plan{opacity:.72}.disabled-plan .btn{pointer-events:none;opacity:.65}
  </style>
</head>
<body>
<canvas id="matrix"></canvas>
<div class="page">
  <header class="wrap topbar">
    <a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil"/> MATRIX REPROGRAMMED</a>
    <nav class="nav"><a href="start-here.html">Start Here</a><a href="daily-command-brief.html">Daily Brief</a><a href="search.html">Search</a><a href="deck-expansion-hub.html">Decks</a><a href="review-dashboard.html">Review</a><a href="store.html">Store</a><a href="membership.html">Membership</a><a href="books.html">Books</a></nav>
  </header>
  <main class="wrap section">
    <div class="eyebrow">Membership System</div>
    <h1>CREATE YOUR MEMBER ACCOUNT.</h1>
    <p class="lead">Join the free member layer now. Paid PayPal tiers will be activated only after subscription verification and protected-access testing are complete.</p>
    <p><strong>Evidence boundary:</strong> Membership gives access to research tools and material. It does not change the evidence standard applied to claims.</p>

    <section class="section money-card">
      <h2>Free membership and Daily Control Brief</h2>
      <p>Create an account using your email. You will receive a secure one-time verification link. No password is required.</p>
      <form id="membership-signup" class="member-form" novalidate>
        <label>Display name <input id="member-name" name="name" type="text" maxlength="120" autocomplete="name" placeholder="Your name (optional)"/></label>
        <label>Email address <input id="member-email" name="email" type="email" maxlength="240" autocomplete="email" required placeholder="you@example.com"/></label>
        <label class="consent-row"><input id="member-consent" name="marketingConsent" type="checkbox" required/><span>I agree to receive the Matrix Reprogrammed member email and Daily Control Brief. I can unsubscribe at any time.</span></label>
        <button class="btn" type="submit">Create Free Member Account</button>
        <p id="signup-status" class="status" role="status" aria-live="polite"></p>
      </form>
      <div class="account-actions">
        <a class="btn" href="member-login.html">Already a member? Sign in</a>
        <a class="btn" href="member-dashboard.html">Open member dashboard</a>
      </div>
      <p class="mini">Verification links expire after 15 minutes and can be used once. Login sessions use secure, HttpOnly cookies.</p>
    </section>

    <section class="section">
      <h2>Planned PayPal membership tiers</h2>
      <p>These tiers remain deliberately disabled until PayPal webhooks can prove payment status before granting access.</p>
      <div class="money-grid">
        <article class="money-card disabled-plan" id="join-supporter"><h3>Supporter</h3><div class="price">€9/month</div><ul><li>weekly member brief</li><li>member newsletter</li><li>sample premium reports</li><li>member-only source drops</li></ul><span class="btn">PayPal activation pending</span></article>
        <article class="money-card disabled-plan" id="join-intelligence-member"><h3>Intelligence Member</h3><div class="price">€19/month</div><ul><li>premium daily brief</li><li>full card intelligence</li><li>full source ledger</li><li>deeper dossiers</li><li>downloadable card decks</li><li>weekly control map</li><li>missing-record queue</li></ul><span class="btn">PayPal activation pending</span></article>
        <article class="money-card disabled-plan" id="join-research-pro"><h3>Research Pro</h3><div class="price">€49/month</div><ul><li>advanced search</li><li>full dossier exports</li><li>monthly PDF intelligence report</li><li>source route maps</li><li>policy tracker</li><li>jurisdiction tracker</li><li>card score movement reports</li><li>priority source requests</li></ul><span class="btn">PayPal activation pending</span></article>
      </div>
    </section>
  </main>
  <footer class="footer wrap"><p><strong>Boundary:</strong> Paid access will not be enabled until PayPal subscription state, cancellation and failed-payment handling are verified.</p></footer>
</div>
<script src="matrix.js"></script>
<script>
(() => {
  const form = document.getElementById('membership-signup');
  const status = document.getElementById('signup-status');
  form.addEventListener('submit', async event => {
    event.preventDefault();
    status.className = 'status';
    status.textContent = 'Creating your member record…';
    const email = document.getElementById('member-email').value.trim();
    const name = document.getElementById('member-name').value.trim();
    const marketingConsent = document.getElementById('member-consent').checked;
    try {
      const response = await fetch('/api/membership/signup', {
        method: 'POST',
        headers: {'content-type':'application/json'},
        body: JSON.stringify({email,name,marketingConsent,consentVersion:'membership-consent-v1',source:'membership-page',sourcePage:'/membership.html'})
      });
      const data = await response.json();
      if (!response.ok || !data.saved) throw new Error(data.error || data.message || 'Account could not be created.');
      status.className = 'status ok';
      status.textContent = data.emailSent ? 'Account saved. Check your email for the one-time verification link.' : 'Account saved, but verification email delivery is not configured yet.';
      if (data.emailSent) form.reset();
    } catch (error) {
      status.className = 'status error';
      status.textContent = error.message || 'Account could not be created.';
    }
  });
})();
</script>
</body>
</html>
