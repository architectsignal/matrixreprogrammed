(() => {
  'use strict';
  // Compatibility asset retained for cached pages. The authoritative result
  // interface now lives in research-tools.js so a completed job is rendered
  // exactly once with one vocabulary and one score interpretation standard.
  if (window.__MATRIX_RESEARCH_UI_AUTHORITATIVE__) return;
})();
