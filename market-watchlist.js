(()=>{const form=document.querySelector('#market-watch-form');const status=document.querySelector('#market-watch-status');const list=document.querySelector('#market-watch-list');let authenticated=false;function setStatus(text,error=false){if(!status)return;status.textContent=text;status.style.borderLeftColor=error?'#b44':'#d8b56a'}async function request(path,options={}){const response=await fetch(path,{credentials:'same-origin',headers:{'content-type':'application/json',...(options.headers||{})},...options});let payload={};try{payload=await response.json()}catch{}if(!response.ok){const error=new Error(payload.error||`HTTP ${response.status}`);error.status=response.status;throw error}return payload}function esc(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function render(items=[]){if(!list)return;if(!items.length){list.innerHTML='<article class="card"><p>No saved market watches yet.</p></article>';return}list.innerHTML=items.map(item=>`<article class="card redline watch-item"><div><span class="label">${esc(item.targetType)} · ${esc(item.targetKey)}</span><h3>${esc(item.targetLabel)}</h3><p>Insider: ${item.alertInsiderTransactions?'on':'off'} · Institutions: ${item.alertInstitutionChanges?'on':'off'} · New positions: ${item.alertNewPositions?'on':'off'} · Exits: ${item.alertExits?'on':'off'}</p><p>Minimum value: ${item.minimumReportedValue==null?'none':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(item.minimumReportedValue)}</p></div><button class="btn alt" data-delete-watch="${esc(item.id)}">Remove</button></article>`).join('');list.querySelectorAll('[data-delete-watch]').forEach(button=>button.addEventListener('click',async()=>{button.disabled=true;try{await request(`/api/market/watchlists/${encodeURIComponent(button.dataset.deleteWatch)}`,{method:'DELETE'});await load()}catch(error){setStatus(error.message,true)}finally{button.disabled=false}}))}async function load(){try{const payload=await request('/api/market/watchlists');authenticated=true;form?.querySelectorAll('input,select,button').forEach(el=>el.disabled=false);setStatus(`Verified member · ${payload.watchlists?.length||0} saved watches.`);render(payload.watchlists||[])}catch(error){authenticated=false;form?.querySelectorAll('input,select,button').forEach(el=>el.disabled=true);if(error.status===401)setStatus('Member login required to create or view watchlists.',true);else setStatus(error.message,true)}}form?.addEventListener('submit',async event=>{event.preventDefault();if(!authenticated)return;const data=new FormData(form);const body={targetType:String(data.get('targetType')||''),targetKey:String(data.get('targetKey')||'').trim(),targetLabel:String(data.get('targetLabel')||'').trim(),alertInsiderTransactions:data.get('alertInsiderTransactions')==='on',alertInstitutionChanges:data.get('alertInstitutionChanges')==='on',alertNewPositions:data.get('alertNewPositions')==='on',alertExits:data.get('alertExits')==='on',minimumReportedValue:data.get('minimumReportedValue')?Number(data.get('minimumReportedValue')):null};const submit=form.querySelector('button[type=submit]');submit.disabled=true;try{await request('/api/market/watchlists',{method:'POST',body:JSON.stringify(body)});form.reset();form.querySelectorAll('input[type=checkbox]').forEach(el=>el.checked=true);setStatus('Watch saved.');await load()}catch(error){setStatus(error.message,true)}finally{submit.disabled=false}});form?.querySelectorAll('input,select,button').forEach(el=>el.disabled=true);load();})();
