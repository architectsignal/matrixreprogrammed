name: PayPal Worker credential normalization hotfix

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/paypal-worker-hotfix-deploy.yml

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Build Cloudflare output with PayPal credential normalization
        run: |
          node scripts/repair-paypal-subscription-create-runtime.js
          npm run cloudflare-output
          node scripts/cloudflare-worker-routes-test.js
      - name: Deploy Worker and assets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@latest deploy
