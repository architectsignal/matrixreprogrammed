const fs=require('fs');
const path=require('path');
const root=process.cwd();const site=path.join(root,'_site');
const checks=[];const add=(name,ok,detail='')=>checks.push({name,ok:Boolean(ok),detail});
const exists=rel=>fs.existsSync(path.join(site,rel));const read=rel=>exists(rel)?fs.readFileSync(path.join(site,rel),'utf8'):'';
for(const rel of ['geographic-power-atlas.html','geographic-power-atlas','geographic-power-atlas.js','data/geographic-power-atlas.json','data/geographic-power-atlas-data.json','data/geographic-power-atlas-seed.json','downloads/geographic-power-atlas.csv','index.html','start-here.html','start-here','research-tools.html','security-privacy.html','dark-web-safety.html','search-index.json','sitemap.xml','llms.txt'])add(`deployable ${rel}`,exists(rel),exists(rel)?'present':'missing');
const page=read('geographic-power-atlas.html');const runtime=read('geographic-power-atlas.js');const home=read('index.html');const startHere=read('start-here.html');const tools=read('research-tools.html');
const seed=JSON.parse(read('data/geographic-power-atlas-seed.json')||'{}');
const maplibreVersion=String(seed.engines?.maplibre?.version||'');
const pmtilesVersion=String(seed.engines?.pmtiles?.version||'');
add('page title marker',page.includes('GEOGRAPHIC POWER ATLAS.'));
add('page is publicly visible',!/<div class="page[^"]*(commercial-internal|internal-only)/i.test(page));
add('MapLibre CSS pin matches deployable seed',maplibreVersion&&page.includes(`maplibre-gl@${maplibreVersion}/dist/maplibre-gl.css`),maplibreVersion);
add('MapLibre JS pin matches deployable seed',maplibreVersion&&page.includes(`maplibre-gl@${maplibreVersion}/dist/maplibre-gl.js`),maplibreVersion);
add('PMTiles pin matches deployable seed',pmtilesVersion&&runtime.includes(`pmtiles@${pmtilesVersion}`),pmtilesVersion);
add('same-origin PMTiles gate',runtime.includes('url.origin === location.origin'));
add('runtime uses deployable atlas JSON',runtime.includes('data/geographic-power-atlas-data.json'));
add('precision boundary visible',/PRECISION IS PART OF THE EVIDENCE/i.test(page));
add('proximity warning visible',/Nearby points are not evidence/i.test(page));
add('homepage atlas route',home.includes('Open Geographic Power Atlas'));
add('research tools atlas route',tools.includes('geographic-power-atlas.html'));
const primaryNav=(home.match(/<div class="nav-primary">([\s\S]*?)<\/div>/i)||[])[1]||'';
add('Security Tools promoted to primary tab bar',primaryNav.includes('href="security-privacy.html"')&&primaryNav.includes('>Security Tools</a>'));
add('Dark Web Safety promoted to primary tab bar',primaryNav.includes('href="dark-web-safety.html"')&&primaryNav.includes('>Dark Web Safety</a>'));
add('primary safety tabs are not hidden',!/(security-privacy\.html|dark-web-safety\.html)[^>]*(internal-only|commercial-internal)/i.test(primaryNav));
const startNavMatch=startHere.match(/<nav\b[^>]*class=["'][^"']*\bnav\b[^"']*["'][^>]*>([\s\S]*?)<\/nav>/i);const startNav=startNavMatch?.[1]||'';
add('Start Here navigation includes Security Tools',startNav.includes('href="security-privacy.html"')&&startNav.includes('>Security Tools</a>'));
add('Start Here navigation includes Dark Web Safety',startNav.includes('href="dark-web-safety.html"')&&startNav.includes('>Dark Web Safety</a>'));
add('Start Here has visible safety cards',startHere.includes('Open Security Tools')&&startHere.includes('Open Dark Web Safety'));
add('Start Here grid is not hidden',!/<section class=["'][^"']*commercial-internal[^"']*["'][^>]*>[\s\S]*?Open Security Tools/i.test(startHere));
const ids=[...startHere.matchAll(/\bid\s*=\s*(["'])([^"']+)\1/gi)].map(match=>match[2]);
add('Start Here IDs are unique',new Set(ids).size===ids.length,`${ids.length-new Set(ids).size} duplicate IDs`);
add('search contains atlas',read('search-index.json').includes('geographic-power-atlas'));
add('sitemap contains atlas',read('sitemap.xml').includes('/geographic-power-atlas.html'));
add('llms contains atlas',read('llms.txt').includes('Geographic Power Atlas'));
const geo=JSON.parse(read('data/geographic-power-atlas-data.json')||'{}');
add('deployable GeoJSON has at least 30 points',geo.type==='FeatureCollection'&&Array.isArray(geo.features)&&geo.features.length>=30,`${geo.features?.length||0}`);
add('no private or personal address fields',(geo.features||[]).every(feature=>!Object.keys(feature.properties||{}).some(key=>/(homeAddress|privateAddress|personalAddress)/i.test(key))));
add('every point has precision and source',(geo.features||[]).every(feature=>feature.properties?.precision&&/^https:\/\//.test(feature.properties?.sourceUrl||'')));
for(const rel of ['downloads/geographic-power-atlas-build.json','downloads/geographic-power-atlas-test.json','downloads/geographic-power-atlas-output-test.json'])add(`private report excluded ${rel}`,!exists(rel));
const report={ok:checks.every(check=>check.ok),generatedAt:new Date().toISOString(),versions:{maplibre:maplibreVersion,pmtiles:pmtilesVersion},checks};
fs.mkdirSync(path.join(root,'downloads'),{recursive:true});fs.writeFileSync(path.join(root,'downloads','geographic-power-atlas-output-test.json'),JSON.stringify(report,null,2));
if(!report.ok){checks.filter(check=>!check.ok).forEach(check=>console.error(`FAILED: ${check.name}${check.detail?` — ${check.detail}`:''}`));process.exit(1);}
console.log(`Geographic Power Atlas deployable output passed: ${checks.length} checks; MapLibre ${maplibreVersion}, PMTiles ${pmtilesVersion}.`);
