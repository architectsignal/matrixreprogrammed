{
  "updated": "2026-06-25T11:31:00Z",
  "purpose": "Harmless deploy trigger marker after adding wrangler.toml with _site output directory.",
  "expected": "Cloudflare should build a commit that includes wrangler.toml and use _site instead of dist.",
  "requiredConfig": "wrangler.toml -> pages_build_output_dir = _site"
}
