const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'lead-magnets.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/lead-magnets.json found. Skipping Phase 19 Lead Magnet build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
const outDir = path.join(root, 'downloads');
if (!fs.existsSync(outDir)) fs.mkdirSync(outDir, { recursive: true });
function esc(s = '') { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
function nav() { return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="optin-center.html">Opt-in Center</a><a href="offer-center.html">Offer Center</a><a href="launch-room.html">Launch Room</a><a href="share-center.html">Share Center</a><a href="download-center.html">Download Center</a><a href="trust-center.html">Trust Center</a><a href="amazon-store-books.html">Amazon Store</a><a href="forum.html">Signal Board</a></nav></header>`; }
function layout(title, desc, body) { return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /><script type="application/ld+json">${JSON.stringify({'@context':'https://schema.org','@type':'WebPage',name:title,description:desc})}</script></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Opt-in boundary: forms collect reader interest only. Evidence pages carry claim strength.</p></footer></div><script src="matrix.js"></script></body></html>`; }
function titleFromFile(file) { return file.replace(/^downloads\//, '').replace(/\.html$|\.json$|\.md$/g, '').replace(/-/g, ' '); }
function buttons(routes = []) { return routes.map(route => `<a class="btn alt" href="${esc(route)}">${esc(titleFromFile(route))}</a>`).join(''); }
function cards(items = []) { return items.map(item => `<article class="card"><span class="label">Included</span><h3>${esc(item)}</h3><p>This deliverable routes back to the archive and evidence boundary.</p></article>`).join(''); }
function magnetJson(m) { return { updated: data.updated, title: m.title, label: m.label, summary: m.summary, boundary: 'Lead magnets are public orientation assets. They do not increase evidence strength or replace trust/privacy pages.', formName: m.formName, primaryRoute: m.primaryRoute, thankYouRoute: m.thankYouRoute, offerRoute: m.offerRoute, campaignRoute: m.campaignRoute, shareRoute: m.shareRoute, downloadRoute: m.downloadRoute, trustRoute: m.trustRoute, privacyRoute: m.privacyRoute, evidenceRoute: m.evidenceRoute, promise: m.promise, deliverables: m.deliverables }; }
function magnetMarkdown(m) { return `# ${m.title}\n\n${m.summary}\n\n## Boundary\n\nLead magnets are public orientation assets. They do not increase evidence strength or replace trust/privacy pages.\n\n## Promise\n\n${m.promise}\n\n## Routes\n\n- Primary: ${m.primaryRoute}\n- Thank you: ${m.thankYouRoute}\n- Offer: ${m.offerRoute}\n- Campaign: ${m.campaignRoute}\n- Share kit: ${m.shareRoute}\n- Download route: ${m.downloadRoute}\n- Trust: ${m.trustRoute}\n- Privacy: ${m.privacyRoute}\n- Evidence: ${m.evidenceRoute}\n\n## Deliverables\n\n${(m.deliverables || []).map(x => `- ${x}`).join('\n')}\n`; }
function formHtml(m) { return `<form class="card redline" name="${esc(m.formName)}" method="POST" data-netlify="true" netlify action="${esc(m.thankYouRoute)}"><input type="hidden" name="form-name" value="${esc(m.formName)}" /><h2>Get the brief</h2><p>${esc(m.promise)}</p><label>Email<br /><input type="email" name="email" required placeholder="you@example.com" /></label><label>Name<br /><input type="text" name="name" placeholder="Reader name" /></label><input type="hidden" name="lead-magnet" value="${esc(m.slug)}" /><button class="btn" type="submit">Send Me The Brief</button><p class="warning">No private evidence is sent. This route points to public archive pages and trust boundaries.</p></form>`; }
function buildMagnet(m) {
  const htmlFile = `optin-${m.slug}.html`;
  const jsonFile = `downloads/lead-magnet-${m.slug}.json`;
  const mdFile = `downloads/lead-magnet-${m.slug}.md`;
  fs.writeFileSync(path.join(root, jsonFile), JSON.stringify(magnetJson(m), null, 2));
  fs.writeFileSync(path.join(root, mdFile), magnetMarkdown(m));
  const routeList = [m.primaryRoute, m.offerRoute, m.campaignRoute, m.shareRoute, m.downloadRoute, m.trustRoute, m.privacyRoute, m.evidenceRoute];
  const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 19 Lead Magnet</div><h1>${esc(m.title)}</h1><p class="lead">${esc(m.summary)}</p><div class="cta-row"><a class="btn" href="${esc(jsonFile)}">JSON Brief</a><a class="btn alt" href="${esc(mdFile)}">Markdown Brief</a><a class="btn alt" href="optin-center.html">Opt-in Center</a></div></section><section class="section wrap split"><div class="terminal">OPT-IN ROOM\n&gt; Lead magnet: ${esc(m.title)}\n&gt; Form: ${esc(m.formName)}\n&gt; Label: ${esc(m.label)}\n&gt; Offer route: ${esc(m.offerRoute)}\n&gt; Trust route: ${esc(m.trustRoute)}\n&gt; Privacy route: ${esc(m.privacyRoute)}\n&gt; Evidence route: ${esc(m.evidenceRoute)}</div>${formHtml(m)}</section><section class="section wrap"><h2>Opt-in Routes</h2><div class="card"><div class="cta-row small">${buttons(routeList)}</div></div></section><section class="section wrap"><h2>What This Brief Includes</h2><div class="grid">${cards(m.deliverables)}</div></section><section class="section wrap"><h2>Downloads</h2><div class="card"><div class="cta-row"><a class="btn" href="${esc(jsonFile)}">JSON Brief</a><a class="btn alt" href="${esc(mdFile)}">Markdown Brief</a></div></div></section></main>`;
  fs.writeFileSync(path.join(root, htmlFile), layout(`${m.title} | Matrix Reprogrammed`, m.summary, body));
  return [htmlFile, jsonFile, mdFile];
}
function patchFile(file, marker, section) { const p = path.join(root, file); if (!fs.existsSync(p)) return; let html = fs.readFileSync(p, 'utf8'); if (html.includes(marker)) return; html = html.replace('</main>', `${section}</main>`); fs.writeFileSync(p, html); }
function addPagesToSitemap(files) { const p = path.join(root, 'sitemap.xml'); if (!fs.existsSync(p)) return; let xml = fs.readFileSync(p, 'utf8'); const add = files.filter(f => f.endsWith('.html') && !xml.includes(`/${f}</loc>`)).map(f => `  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated)}</lastmod><changefreq>weekly</changefreq><priority>0.9</priority></url>`).join('\n'); if (add) xml = xml.replace('</urlset>', `${add}\n</urlset>`); fs.writeFileSync(p, xml); }
function patchLlms(files) { const p = path.join(root, 'llms.txt'); if (!fs.existsSync(p)) return; let llms = fs.readFileSync(p, 'utf8'); if (llms.includes('/optin-center.html')) return; fs.writeFileSync(p, `${llms.trim()}\n\nPhase 19 Lead Magnet / Capture Engine:\n- /optin-center.html: opt-in hub connecting free briefs, Netlify forms, offers, campaigns, trust pages, privacy, and evidence lanes.\n${files.map(f => `- /${f}: generated opt-in route or downloadable lead-magnet brief.`).join('\n')}\n`); }
function patchSearchIndex(files) { const p = path.join(root, 'search-index.json'); if (!fs.existsSync(p)) return; const search = JSON.parse(fs.readFileSync(p, 'utf8')); const existing = new Set(search.map(i => i.url)); for (const m of data.magnets || []) { const url = `optin-${m.slug}.html`; if (!existing.has(url)) { search.push({ key: `optin-${m.slug}`, title: `${m.title} | Opt-in`, subtitle: m.label, series: 'Lead Magnet Engine', category: 'Email Capture', url, description: m.summary, keywords: [m.title, m.label, m.summary, m.primaryRoute, m.offerRoute, m.formName] }); existing.add(url); } } if (!existing.has('optin-center.html')) search.push({ key: 'optin-center', title: 'Opt-in Center | Matrix Reprogrammed', subtitle: 'Phase 19 Lead Magnet Engine', series: 'Lead Magnet Engine', category: 'Email Capture', url: 'optin-center.html', description: 'Lead magnet hub for Matrix Reprogrammed free briefs and opt-in routes.', keywords: ['optin center','lead magnet','email capture','free brief','newsletter','reader list'] }); fs.writeFileSync(p, JSON.stringify(search, null, 2)); }
const outputs = [];
for (const m of data.magnets || []) outputs.push(...buildMagnet(m));
const magnetCards = (data.magnets || []).map(m => `<article class="card redline"><span class="label">${esc(m.label)}</span><h3>${esc(m.title)}</h3><p>${esc(m.summary)}</p><a class="btn" href="optin-${esc(m.slug)}.html">Open Opt-in</a></article>`).join('');
const ruleCards = (data.rules || []).map(rule => `<article class="card"><span class="label">Capture Rule</span><h3>${esc(rule)}</h3><p>Capture routes must preserve privacy, trust, and evidence boundaries.</p></article>`).join('');
const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 19 Lead Magnet / Capture Engine</div><h1>OPT-IN CENTER.</h1><p class="lead">Free briefs and Netlify forms that move readers from curiosity into offers, campaigns, books, trust pages, and evidence routes.</p><div class="cta-row"><a class="btn" href="optin-black-file-brief.html">Black File Brief</a><a class="btn alt" href="optin-dog-architect-initiation.html">D.O.G Brief</a><a class="btn alt" href="optin-full-archive-map.html">Full Archive Map</a></div></section><section class="section wrap split"><div class="terminal">LEAD MAGNET ENGINE STATUS\n&gt; Opt-ins: ${(data.magnets || []).length}\n&gt; Rules: ${(data.rules || []).length}\n&gt; Netlify forms: ${(data.magnets || []).length}\n&gt; JSON briefs: ${(data.magnets || []).length}\n&gt; Markdown briefs: ${(data.magnets || []).length}\n&gt; Privacy route: active\n&gt; Trust route: active</div><aside class="card redline"><h2>Capture Boundary</h2><p>Opt-ins capture reader interest. They do not replace privacy, trust, or evidence pages.</p></aside></section><section class="section wrap"><h2>Opt-in Routes</h2><div class="grid">${magnetCards}</div></section><section class="section wrap"><h2>Capture Rules</h2><div class="grid">${ruleCards}</div></section></main>`;
fs.writeFileSync(path.join(root, 'optin-center.html'), layout('Opt-in Center | Matrix Reprogrammed', 'Lead magnet hub for Matrix Reprogrammed free briefs and opt-in routes.', body));
outputs.push('optin-center.html');
const section = `<section id="phase-nineteen-lead-magnet-engine" class="section wrap"><h2>Phase 19 Lead Magnet / Capture Engine</h2><p class="lead">This route now connects to free briefs, opt-in forms, privacy boundaries, trust pages, offer paths, and evidence lanes.</p><div class="cta-row"><a class="btn" href="optin-center.html">Opt-in Center</a><a class="btn alt" href="optin-black-file-brief.html">Black File Brief</a><a class="btn alt" href="optin-full-archive-map.html">Full Archive Map</a></div></section>`;
for (const file of ['index.html','offer-center.html','launch-room.html','share-center.html','download-center.html','trust-center.html','black-file.html']) patchFile(file, 'id="phase-nineteen-lead-magnet-engine"', section);
addPagesToSitemap(outputs);
patchLlms(outputs);
patchSearchIndex(outputs);
console.log(`Built Phase 19 Lead Magnets with ${(data.magnets || []).length} opt-ins and ${outputs.length} outputs.`);
