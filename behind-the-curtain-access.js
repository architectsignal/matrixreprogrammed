(()=>{'use strict';const DATA='data/behind-the-curtain-family-access.json';let model=null;const state={query:'',type:'all',confidence:'all'};const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const label=v=>String(v||'').replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());const list=v=>Array.isArray(v)&&v.length?`<ul>${v.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:'<p class="muted">No current record.</p>';function populate(){const types=[...new Set(model.families.map(x=>x.type))].sort();const conf=[...new Set(model.families.map(x=>x.confidence))].sort();$('#acl-type').innerHTML='<option value="all">All family structures</option>'+types.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');$('#acl-confidence').innerHTML='<option value="all">All confidence levels</option>'+conf.map(x=>`<option value="${esc(x)}">${esc(label(x))}</option>`).join('')}function filtered(){return model.families.filter(x=>{if(state.type!=='all'&&x.type!==state.type)return false;if(state.confidence!=='all'&&x.confidence!==state.confidence)return false;if(!state.query)return true;return [x.name,x.type,...x.jurisdictions,...x.structuresReached,...x.documentedAccess].join(' ').toLowerCase().includes(state.query)})}function bars(x){return Object.entries(x.dimensions||{}).map(([k,v])=>`<div class="acl-bar"><span>${esc(label(k))}</span><strong>${esc(v)}</strong><i style="--v:${Math.max(0,Math.min(100,Number(v)))}%"></i></div>`).join('')}function renderFamilies(){const target=$('#acl-families');const items=filtered();if(!items.length){target.innerHTML='<article class="card"><h3>No matching families</h3><p>Change the filters or search terms.</p></article>';return}target.innerHTML=items.map(x=>`<article class="card redline acl-family"><div class="acl-head"><div><span class="acl-rank">#${esc(x.rank)}</span><span class="acl-pill">${esc(label(x.classification))}</span><span class="acl-pill">${esc(label(x.confidence))} confidence</span><h3>${esc(x.name)}</h3><p class="muted">${esc(x.type)} · ${esc(x.jurisdictions.join(' · '))}</p></div><div class="acl-score">${esc(x.accessScore)}<small>/100</small></div></div><div class="acl-bars">${bars(x)}</div><p><strong>Structures reached:</strong> ${esc(x.structuresReached.join(' · '))}</p><details><summary>Open full access assessment</summary><div class="acl-grid"><section><h4>Documented access</h4>${list(x.documentedAccess)}</section><section><h4>Constraints</h4>${list(x.constraints)}</section></div><p><strong>Strongest counterargument:</strong> ${esc(x.strongestCounterargument)}</p><p><strong>Claim not supported:</strong> ${esc(x.unsupportedClaim)}</p><div class="acl-source"><strong>Source ledger</strong><br>${sourceLinks(x.sourceIds)}</div></details></article>`).join('')}function sourceLinks(ids){const index=new Map(model.sources.map(x=>[x.id,x]));return(ids||[]).map(id=>index.get(id)).filter(Boolean).map(s=>`<a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">${esc(s.publisher)}: ${esc(s.title)}</a>`).join('<br>')||'<span class="muted">Source resolution pending</span>'}function renderMatrix(){$('#acl-matrix-body').innerHTML=model.structureAccessMatrix.map(x=>`<tr><td><strong>${esc(x.structure)}</strong></td><td>${list(x.formalControllers)}</td><td>${esc(x.familyAccess)}</td><td>${esc(label(x.confidence))}</td></tr>`).join('')}function renderHistory(){$('#acl-history').innerHTML=model.historicalArchitects.map(x=>`<article class="card redline"><span class="acl-pill">${esc(label(x.classification))}</span><h3>${esc(x.name)}</h3><p><strong>Documented history:</strong> ${esc(x.documentedHistory)}</p><p><strong>Present assessment:</strong> ${esc(x.presentAssessment)}</p></article>`).join('')}function renderSources(){$('#acl-sources').innerHTML=model.sources.map(s=>`<article class="acl-source"><span class="acl-pill">Primary source</span><h3>${esc(s.title)}</h3><p>${esc(s.publisher)}</p><p><strong>Establishes:</strong> ${esc(s.establishes)}</p><p><strong>Does not establish:</strong> ${esc(s.doesNotEstablish)}</p><a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">Open source record</a></article>`).join('')}function bind(){$('#acl-search')?.addEventListener('input',e=>{state.query=e.target.value.trim().toLowerCase();renderFamilies()});$('#acl-type')?.addEventListener('change',e=>{state.type=e.target.value;renderFamilies()});$('#acl-confidence')?.addEventListener('change',e=>{state.confidence=e.target.value;renderFamilies()})}async function init(){try{const r=await fetch(`${DATA}?v=20260722`,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);model=await r.json();$('#acl-boundary').textContent=model.editorialBoundary;$('#acl-conclusion').innerHTML=`<p>${esc(model.finalConclusion)}</p>`;populate();bind();renderFamilies();renderMatrix();renderHistory();renderSources()}catch(error){console.error(error);$('#acl-conclusion').innerHTML='<p>The Access Layer evidence package could not be loaded. The page fails closed rather than inventing a conclusion.</p>';$('#acl-families').innerHTML='<article class="card redline"><h3>Evidence package unavailable</h3><p>No family ranking is displayed without the reviewed source model.</p></article>'}}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init()})();