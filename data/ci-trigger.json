{
  "updated": "2026-06-27T00:45:00+02:00",
  "purpose": "Deploy trigger after hiding visible compatibility marker text on the homepage.",
  "deployIntent": "Build and deploy the complete upgraded Cloudflare site from current main.",
  "criticalFix": "Compatibility pressure-test markers that leaked as reader-visible text are now scrubbed from visible HTML and reinserted inside a hidden marker vault by scripts/hide-visible-compatibility-markers.js.",
  "includedUpgrades": [
    "visible compatibility marker cleanup",
    "hidden CI marker vault for preserved test strings",
    "all-seeing-eye homepage footer gate to dark-speculation-lab.html",
    "dark-speculation-lab.html root fallback page",
    "dark-speculation-forum.html root fallback page",
    "obvious speculation nav links removed from fallback pages",
    "speculation pages removed from visible search/sitemap/llms discovery during build",
    "branded PDF mini-book covers and table of contents",
    "subject intelligence profile PDFs and HTML hubs",
    "source-card-index compatibility alias",
    "Link Audit builds generated site before auditing"
  ],
  "cloudflareExpected": [
    "npm run build",
    "scripts/hide-visible-compatibility-markers.js runs after compatibility restore",
    "build-cloudflare-output.js copies deployable files into _site",
    "wrangler deploy serves _site through src/worker.js",
    "homepage bottom should show normal footer plus one all-seeing eye, not raw marker text"
  ],
  "expected": "Fresh push should trigger connected Cloudflare build/deploy against current main."
}
