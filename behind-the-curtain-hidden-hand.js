(()=>{'use strict';
const URLS={
 index:'data/behind-the-curtain-intermediaries-index.json',
 profiles:['data/behind-the-curtain-intermediaries-01.json','data/behind-the-curtain-intermediaries-02.json','data/behind-the-curtain-intermediaries-03.json'],
 sources:'data/behind-the-curtain-intermediary-sources.json',
 health:'data/behind-the-curtain-intermediary-source-health.json',
 continuity:'data/behind-the-curtain-continuity-systems.json',
 models:'data/behind-the-curtain-hidden-hand-models.json',
 families:'data/behind-the-curtain-family-principals.json',
 symbolic:'data/behind-the-curtain-symbolic-research.json'
};
const state={mode:'structural',profiles:[],index:null,sources:new Map(),health:null};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const arr=v=>Array.isArray(v)?v:[];
const label=v=>String(v||'').replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
const fetchJson=async url=>{const r=await fetch(`${url}?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(`${url}: ${r.status}`);return r.json()};
const badge=v=>`<span class="hh-badge hh-${esc(v)}">${esc(label(v))}</span>`;
const list=(items,cls='')=>arr(items).length?`<ul class="${cls}">${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:'<p class="muted">No reviewed record.</p>';
function sourceCards(ids){return arr(ids).map(id=>state.sources.get(id)).filter(Boolean).map(s=>`<article class="hh-source"><span>${esc(s.publisher)}</span><h4>${esc(s.title)}</h4><p><b>Establishes:</b> ${esc(s.establishes)}</p><p><b>Does not establish:</b> ${esc(s.doesNotEstablish)}</p><a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">Open record</a></article>`).join('')||'<p class="muted">No resolved source in this package.</p>'}
function injectStyles(){if(document.querySelector('#hidden-hand-styles'))return;const style=document.createElement('style');style.id='hidden-hand-styles';style.textContent=`
.hh-section{position:relative;padding-block:clamp(3.5rem,8vw,7rem)}.hh-section:before{content:"";position:absolute;inset:8% -12%;background:radial-gradient(circle at 20% 18%,rgba(225,187,97,.08),transparent 26%),radial-gradient(circle at 82% 70%,rgba(179,39,59,.09),transparent 30%);pointer-events:none}.hh-head{position:relative;max-width:900px;margin-bottom:1.4rem}.hh-head h2{font-size:clamp(2.1rem,5vw,4.5rem);line-height:.95;margin:.35rem 0}.hh-head p{font-size:1.08rem;color:#bdbdbd}.hh-toolbar{display:flex;gap:.6rem;flex-wrap:wrap;margin:1rem 0 1.25rem}.hh-toolbar input,.hh-toolbar select{background:#050505;color:#eee;border:1px solid rgba(225,187,97,.35);border-radius:999px;padding:.7rem .9rem;min-width:230px}.hh-grid{position:relative;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem}.hh-card{border:1px solid rgba(225,187,97,.28);border-radius:19px;padding:1.1rem;background:linear-gradient(140deg,rgba(20,6,9,.92),rgba(4,4,5,.96));box-shadow:0 22px 70px rgba(0,0,0,.22)}.hh-card[hidden]{display:none}.hh-top{display:flex;justify-content:space-between;gap:.75rem;align-items:start}.hh-rank{font-size:1.7rem;color:#e1bb61;font-weight:900}.hh-score{font-size:1.45rem;color:#f3dfaa;font-weight:900}.hh-card h3{font-size:1.35rem;margin:.15rem 0}.hh-role{color:#eee}.hh-lane{color:#d6b96f;font-size:.78rem;text-transform:uppercase;letter-spacing:.09em}.hh-badge{display:inline-flex;border:1px solid currentColor;border-radius:999px;padding:.2rem .46rem;font-size:.65rem;text-transform:uppercase;letter-spacing:.06em}.hh-documented_fact{color:#dcdcdc}.hh-strongly_supported_assessment{color:#e1bb61}.hh-plausible_structural_inference{color:#d8863e}.hh-disputed{color:#bb8cff}.hh-symbolic_or_mythological{color:#f2d78d;border-style:dashed}.hh-unsupported{color:#9299a2}.hh-meters{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.32rem;margin:.75rem 0}.hh-meter{font-size:.68rem;color:#aaa}.hh-meter i{display:block;height:3px;margin-top:.18rem;background:linear-gradient(90deg,#e1bb61 var(--v),#262626 var(--v));border-radius:99px}.hh-card details{border-top:1px solid #292929;margin-top:.8rem;padding-top:.7rem}.hh-card summary{cursor:pointer;color:#f3dfaa;font-weight:800}.hh-boundary{border-left:4px solid #b3273b;padding:.65rem .8rem;margin-top:.8rem;background:rgba(26,5,9,.65);color:#c8c8c8}.hh-source-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.65rem;margin-top:.75rem}.hh-source{border:1px solid #303030;border-radius:13px;padding:.75rem;background:#030303}.hh-source span{color:#d6b96f;font-size:.72rem;text-transform:uppercase}.hh-source h4{margin:.2rem 0}.hh-source p{font-size:.84rem;color:#bbb}.hh-source a{color:#f0d28b}.hh-model{position:relative;overflow:hidden}.hh-model:after{content:attr(data-rank);position:absolute;right:.5rem;bottom:-1.2rem;font-size:6rem;font-weight:900;color:rgba(225,187,97,.035)}.hh-system-row{display:grid;grid-template-columns:minmax(190px,.6fr) minmax(0,1.4fr);gap:1rem;border-top:1px solid #2c2c2c;padding:1rem 0}.hh-system-row h3{margin:0}.hh-family-names{display:flex;gap:.4rem;flex-wrap:wrap;margin:.6rem 0}.hh-name{border:1px solid #3b3b3b;border-radius:999px;padding:.32rem .52rem;background:#070707}.hh-symbolic{border-style:dashed;background:radial-gradient(circle at 50% 0,rgba(225,187,97,.12),transparent 40%),#050504}.hh-health{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:.65rem;margin:1rem 0}.hh-health article{border:1px solid #303030;border-radius:14px;padding:.85rem;background:#050505}.hh-health strong{display:block;color:#e1bb61;font-size:1.25rem}.hh-empty{border:1px solid #6d202b;border-radius:16px;padding:1rem;background:#170508}.hh-mode-hidden{display:none!important}@media(max-width:850px){.hh-grid,.hh-source-grid{grid-template-columns:1fr}.hh-system-row{grid-template-columns:1fr}.hh-health{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:520px){.hh-health{grid-template-columns:1fr}.hh-toolbar input,.hh-toolbar select{width:100%;min-width:0}}
`;document.head.append(style)}
function section(id,eyebrow,title,copy,body){const s=document.createElement('section');s.id=id;s.className='section wrap hh-section';s.innerHTML=`<div class="hh-head"><div class="eyebrow">${esc(eyebrow)}</div><h2>${esc(title)}</h2><p>${esc(copy)}</p></div>${body}`;return s}
function profileCard(p){const dims=Object.entries(p.dimensions||{}).map(([k,v])=>`<span class="hh-meter">${esc(label(k))}<i style="--v:${Math.max(0,Math.min(100,Number(v)||0))}%"></i></span>`).join('');return `<article class="hh-card hh-profile" data-name="${esc((p.name+' '+p.currentRole+' '+p.lane).toLowerCase())}" data-lane="${esc(p.lane)}" data-classification="${esc(p.classification)}"><div class="hh-top"><span class="hh-rank">#${esc(p.rank)}</span><span class="hh-score">${esc(p.accessScore)}</span></div><div class="hh-lane">${esc(p.lane)}</div><h3>${esc(p.name)}</h3><p class="hh-role">${esc(p.currentRole)}</p>${badge(p.classification)}<div class="hh-meters">${dims}</div><details><summary>Who this person can reach</summary>${list(p.accessTo)}<h4>Access mechanisms</h4>${list(p.mechanisms)}<h4>Documented reach</h4>${list(p.documentedReach)}</details><details><summary>Limits and counter-evidence</summary>${list(p.constraints)}<div class="hh-boundary"><b>Strongest counterargument:</b> ${esc(p.strongestCounterargument)}</div></details><details><summary>Source records</summary><div class="hh-source-grid">${sourceCards(p.sourceIds)}</div></details></article>`}
function renderGatekeepers(){const lanes=[...new Set(state.profiles.map(p=>p.lane))].sort();const body=`<div class="hh-toolbar"><input id="hh-search" type="search" placeholder="Search names, roles or access lanes" aria-label="Search hidden hand profiles"><select id="hh-lane"><option value="">All access lanes</option>${lanes.map(x=>`<option>${esc(x)}</option>`).join('')}</select></div><div class="hh-health"><article><strong>${state.profiles.length}</strong><span>named access intermediaries</span></article><article><strong>${state.index.continuitySystems.length}</strong><span>continuity systems</span></article><article><strong>${state.index.hiddenHandModels.length}</strong><span>tested hidden-hand models</span></article><article><strong>${state.sources.size}</strong><span>bounded source records</span></article></div><div id="hh-profile-grid" class="hh-grid">${state.profiles.sort((a,b)=>a.rank-b.rank).map(profileCard).join('')}</div>`;const s=section('gatekeepers','Access to the Access-Holders','WHO GETS INTO THE ROOM?','Public leaders possess formal authority. These profiles name the people whose offices control briefing flow, meeting access, staff coordination, appointments, committee preparation, sovereign capital, security information or institutional execution.',body);document.querySelector('#chokepoints')?.before(s);const filter=()=>{const q=document.querySelector('#hh-search').value.trim().toLowerCase(),lane=document.querySelector('#hh-lane').value;document.querySelectorAll('.hh-profile').forEach(c=>{const hit=(!q||c.dataset.name.includes(q))&&(!lane||c.dataset.lane===lane);c.hidden=!hit})};s.querySelector('#hh-search').addEventListener('input',filter);s.querySelector('#hh-lane').addEventListener('change',filter)}
function renderModels(){const cards=state.index.hiddenHandModels.sort((a,b)=>a.rank-b.rank).map(m=>`<article class="hh-card hh-model" data-rank="${esc(m.rank)}" data-classification="${esc(m.classification)}">${badge(m.classification)}<h3>${esc(m.name)}</h3><p>${esc(m.thesis)}</p><details open><summary>Evidence for</summary>${list(m.evidenceFor)}</details><details><summary>Evidence against</summary>${list(m.evidenceAgainst)}</details><p><b>What would prove more:</b> ${esc(m.whatWouldProveMore)}</p><div class="hh-boundary">${esc(m.boundary)}</div></article>`).join('');const s=section('hidden-hand','Nine Competing Models','THE HIDDEN HAND — TESTED, NOT ASSUMED.','The page does not force every connection into one theory. Each model is graded, challenged and paired with evidence that would strengthen or falsify it.',`<div class="hh-grid">${cards}</div>`);document.querySelector('#pathways')?.after(s)}
function renderSystems(){const rows=state.index.continuitySystems.map(x=>`<article class="hh-system-row" data-classification="${esc(x.classification)}"><div>${badge(x.classification)}<h3>${esc(x.name)}</h3><p>${esc(x.status)}</p></div><div><p>${esc(x.mechanism)}</p><p><b>Named examples:</b> ${esc(arr(x.namedExamples).join(' · '))}</p><details><summary>Evidence and limits</summary><h4>Evidence for</h4>${list(x.evidenceFor)}<h4>Evidence against</h4>${list(x.evidenceAgainst)}<div class="hh-boundary">${esc(x.boundary)}</div></details></div></article>`).join('');const s=section('permanent-system','Deep State, Defined','WHAT SURVIVES THE ELECTION?','“Deep state” is decomposed into named, testable continuity systems: permanent offices, security councils, central-bank boards, technical secretariats, sovereign funds, corporate platforms and dynastic governance.',rows);document.querySelector('#councils')?.before(s)}
function renderFamilies(){const cards=state.index.familyPrincipals.map(f=>`<article class="hh-card" data-classification="${esc(f.classification)}">${badge(f.classification)}<h3>${esc(f.familyName)}</h3><div class="hh-family-names">${arr(f.namedPeople).map(p=>`<span class="hh-name"><b>${esc(p.name)}</b> · ${esc(p.role)}</span>`).join('')}</div><p><b>Continuity mechanism:</b> ${esc(f.mechanism)}</p><div class="hh-boundary">${esc(f.boundary)}</div><details><summary>Sources</summary><div class="hh-source-grid">${sourceCards(f.sourceIds)}</div></details></article>`).join('');const s=section('family-principals','The People Inside the Dynasties','FAMILY NAMES ARE NOT ENOUGH.','Each dynasty is connected to named office-holders, board members, voting-pool representatives, foundation principals or holding-company leaders. A surname alone proves nothing.',`<div class="hh-grid">${cards}</div>`);document.querySelector('#dynasties')?.after(s)}
function renderSymbolicResearch(){const cards=state.index.symbolicResearch.map(x=>`<article class="hh-card hh-symbolic" data-classification="${esc(x.classification)}">${badge(x.classification)}<h3>${esc(x.name)}</h3><p><b>Historical finding:</b> ${esc(x.historicalFinding)}</p><p><b>Modern interpretive use:</b> ${esc(x.modernInterpretiveUse)}</p><div class="hh-boundary"><b>Not established:</b> ${esc(x.notEstablished)}</div><details><summary>Source record</summary><div class="hh-source-grid">${sourceCards(x.sourceIds)}</div></details></article>`).join('');const s=section('symbolic-research','Historical Text Before Internet Myth','BAAL · MOLOCH · SATURN · LUCIFER · THE LIGHTBRINGER','These names are examined as historical, theological, psychological and political ideas. They are not treated as proven operators in a modern command chain.',`<div class="hh-grid">${cards}</div>`);document.querySelector('#lightbringer')?.before(s)}
function applyMode(mode){state.mode=mode;document.querySelectorAll('[data-classification]').forEach(el=>{const c=el.dataset.classification;const hide=mode==='proof'&&['plausible_structural_inference','disputed','symbolic_or_mythological','unsupported'].includes(c)||mode==='structural'&&c==='symbolic_or_mythological';el.classList.toggle('hh-mode-hidden',hide)})}
function addNav(){const rail=document.querySelector('.pyr-rail-inner');if(!rail)return;const firstButton=rail.querySelector('button');[['#gatekeepers','Gatekeepers'],['#hidden-hand','Hidden Hand'],['#permanent-system','Permanent System'],['#family-principals','Family Principals'],['#symbolic-research','Symbolic History']].forEach(([href,text])=>{if(rail.querySelector(`a[href="${href}"]`))return;const a=document.createElement('a');a.href=href;a.textContent=text;rail.insertBefore(a,firstButton)})}
function healthSummary(){if(!state.health)return;const freshness=document.querySelector('#freshness-grid');if(!freshness)return;const flags=arr(state.health.flags||state.health.findings).length;const card=document.createElement('article');card.className='fresh-card';card.innerHTML=`<strong>${flags===0?'CLEAR':esc(flags)}</strong><span>intermediary source-watch flags</span>`;freshness.append(card)}
async function init(){injectStyles();try{const [index,sources,health,continuitySystems,hiddenHandModels,familyPrincipals,symbolicResearch,...groups]=await Promise.all([fetchJson(URLS.index),fetchJson(URLS.sources),fetchJson(URLS.health),fetchJson(URLS.continuity),fetchJson(URLS.models),fetchJson(URLS.families),fetchJson(URLS.symbolic),...URLS.profiles.map(fetchJson)]);state.index={...index,continuitySystems,hiddenHandModels,familyPrincipals,symbolicResearch};state.profiles=groups.flat();state.sources=new Map(arr(sources).map(s=>[s.id,s]));state.health=health;if(state.profiles.length!==24)throw new Error('Expected 24 reviewed intermediaries');renderGatekeepers();renderModels();renderSystems();renderFamilies();renderSymbolicResearch();addNav();healthSummary();document.addEventListener('click',e=>{const b=e.target.closest('[data-view-mode]');if(b)applyMode(b.dataset.viewMode)});applyMode('structural')}catch(err){console.error(err);const marker=document.createElement('section');marker.className='section wrap hh-section';marker.innerHTML='<div class="hh-empty"><h2>Hidden-hand evidence layer failed closed.</h2><p>No names or conclusions are invented when a reviewed data shard or source-health record is unavailable.</p></div>';document.querySelector('#chokepoints')?.before(marker)}}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();
