name: Retired homepage intro dispatcher

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: retired-homepage-intro-dispatcher
  cancel-in-progress: false

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm controlled production boundary
        run: |
          echo 'This one-shot homepage intro dispatcher is retired.'
          echo 'All production releases must use Matrix Reprogrammed Controlled Production Deploy with explicit owner confirmation.'
