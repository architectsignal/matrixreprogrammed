const fs=require('fs');const path=require('path');const {spawnSync}=require('child_process');
const root=process.cwd();const reports=path.join(root,'downloads');fs.mkdirSync(reports,{recursive:true});
for(const name of ['search-semantic-vector.js','search-hybrid-engine.js']){const source=path.join(root,'scripts',name);if(!fs.existsSync(source))throw new Error(`${source} missing`);fs.copyFileSync(source,path.join(root,name));}
function run(script){const result=spawnSync(process.execPath,[path.join(root,'scripts',script)],{cwd:root,encoding:'utf8',stdio:'pipe'});if(result.stdout)process.stdout.write(result.stdout);if(result.stderr)process.stderr.write(result.stderr);if(result.status!==0)process.exit(result.status||1);}
run('build-search-semantic-index.js');run('check-search-provider-entitlements.js');
const htmlPath=path.join(root,'search.html');if(!fs.existsSync(htmlPath))throw new Error('search.html missing');let html=fs.readFileSync(htmlPath,'utf8');
html=html.replace(/<script src="search-semantic-vector\.js"><\/script>/g,'').replace(/<script src="search-hybrid-engine\.js"><\/script>/g,'');
const qualityTag='<script src="search-quality-engine.js"></script>';if(!html.includes(qualityTag))throw new Error('search-quality-engine.js script marker missing');
html=html.replace(qualityTag,`<script src="search-semantic-vector.js"></script>${qualityTag}<script src="search-hybrid-engine.js"></script>`);
html=html.replace(/Search Quality V1 · Consequence-Aware/g,'Search Hybrid V1 · Consequence-Aware');
fs.writeFileSync(htmlPath,html);
const runtime=`(function(){
'use strict';
// SEARCH V2 compatibility marker. Hybrid V1 keeps exact/entity/BM25 retrieval authoritative and adds compact local semantic vectors.
const input=document.getElementById("archive-search"),results=document.getElementById("search-results"),count=document.getElementById("search-count"),answer=document.getElementById("ask-answer"),shortcuts=document.getElementById("ask-shortcuts");
if(!input||!results)return;
const hybrid=globalThis.MatrixHybridSearch;
if(!hybrid){if(answer)answer.textContent="SEARCH HYBRID ERROR\n> Hybrid search engine is unavailable.";return;}
const layerMap=globalThis.MatrixSearchQuality?.DOMAINS||{};
const fallbackIndex=[
{title:"Control Structure Map",url:"control-structure.html",category:"Main Mission",layer:"control-structure",description:"Open the seven-layer control map.",keywords:["control","power","structure"],priority:100},
{title:"Daily Brain Brief",url:"daily-brain-brief.html",category:"Living Brain",layer:"information-narrative",description:"Open the latest conclusions and watch list.",keywords:["daily","brain","brief"],priority:98},
{title:"Evidence Vault",url:"evidence-vault.html",category:"Evidence",layer:"disclosure-black-files",description:"Follow source documents and evidence routes.",keywords:["evidence","records","documents"],priority:92},
{title:"Books",url:"books.html",category:"Books",layer:"general",description:"Open the Matrix Reprogrammed book archive.",keywords:["books","archive"],priority:80}
];
function esc(s){return String(s||"").replace(/[&<>"']/g,function(c){return c==="&"?"&amp;":c==="<"?"&lt;":c===">"?"&gt;":c==='"'?"&quot;":"&#39;";});}
function pct(value){return Math.round(Number(value||0)*100)+"%";}
function card(i){const pills=[i.category,i.layer,i.sourceType].filter(Boolean).slice(0,3).map(x=>"<span class=\"pill\">"+esc(x)+"</span>").join("");const why=(i._reasons||[]).filter(r=>!/conflict/i.test(r)).slice(0,3).join(" · ");return "<article class=\"card redline\"><span class=\"label\">"+esc(i.category||"Route")+"</span><h3>"+esc(i.title)+"</h3><p>"+esc(i.description||"Open this route for deeper context.")+"</p>"+(why?"<p><strong>Why this matched:</strong> "+esc(why)+"</p>":"")+"<p>"+pills+"</p><div class=\"cta-row small\"><a class=\"btn\" href=\""+esc(i.url)+"\">Open Route</a><a class=\"btn alt\" href=\"control-structure.html\">Control Map</a><a class=\"btn alt\" href=\"evidence-vault.html\">Evidence</a></div></article>";}
function noMatch(outcome){const i=outcome.interpretation;const missing=(i.missingContext||[]).length?"\n> Useful details: "+i.missingContext.join(", "):"";if(answer)answer.textContent=["SEARCH CONFIDENCE BOUNDARY","> I understood: "+(i.corrected||outcome.query),"> Subject: "+i.domainLabel,"> Confidence: "+pct(outcome.confidence),"> No reliable evidence-backed match was found."+missing,"> Matrix will not substitute an unrelated card."].join("\n");if(count)count.textContent="No strong route found";results.innerHTML="<article class=\"card redline\"><h3>No reliable match found</h3><p>Matrix understood this as <strong>"+esc(i.corrected||outcome.query)+"</strong>, but the current evidence index does not contain a strong enough answer.</p><p>Add location, supplier, date, organisation or policy details, or submit the question as an unanswered investigation.</p><div class=\"cta-row small\"><a class=\"btn\" href=\"public-record-intake.html\">Open Source Intake</a><a class=\"btn alt\" href=\"forum.html\">Submit Evidence</a><a class=\"btn alt\" href=\"evidence-vault.html\">Evidence Vault</a></div></article>";}
function render(index,q,semanticIndex){q=String(q||"").trim();if(!q){const initial=index.slice().sort((a,b)=>Number(b.priority||0)-Number(a.priority||0)).slice(0,24);if(count)count.textContent="Showing "+initial.length+" verified entry routes";results.innerHTML=initial.map(card).join("");if(answer)answer.textContent=["SEARCH HYBRID V1","> Exact phrase and entity search: active","> BM25 lexical retrieval: active","> Compact local semantic vectors: active","> Domain filtering and reranking: active","> Confidence boundary: active","> Managed AI dependency: none"].join("\n");return;}const outcome=hybrid.search(index,q,{semanticIndex:semanticIndex,limit:36,minScore:4.6,minConfidence:0.36,minSemantic:0.12});if(!outcome.strong||!outcome.results.length){noMatch(outcome);return;}const i=outcome.interpretation;if(count)count.textContent="Found "+outcome.results.length+" relevant route"+(outcome.results.length===1?"":"s");results.innerHTML=outcome.results.map(card).join("");if(answer)answer.textContent=["SEARCH HYBRID RESULT","> Original: "+q,"> Understood: "+i.corrected,"> Subject: "+i.domainLabel,"> Confidence: "+pct(outcome.confidence),"> Best route: "+outcome.results[0].title,"> Retrieval: exact + BM25 + local semantic + domain reranking","> Boundary: relevance is not proof; follow the source route."].join("\n");}
function init(index,semanticIndex){index=Array.isArray(index)?index:[];/* investigationQueryPrefill */try{const q=new URLSearchParams(location.search).get("q");if(q&&!input.value)input.value=q;}catch(_){}function run(){render(index,input.value.trim(),semanticIndex);}input.addEventListener("input",run);if(shortcuts)shortcuts.addEventListener("click",function(e){const b=e.target.closest("button[data-q]");if(!b)return;input.value=b.dataset.q||"";run();input.focus();});run();}
async function fetchJson(url,required){const response=await fetch(url,{cache:'no-store',headers:{Accept:'application/json'}});const type=String(response.headers.get('content-type')||'').toLowerCase();const text=await response.text();if(!response.ok)throw new Error('HTTP '+response.status+' for '+url);if(!type.includes('application/json')||/^\s*</.test(text))throw new Error('HTML returned instead of JSON for '+url);let parsed;try{parsed=JSON.parse(text);}catch(e){throw new Error('Invalid search JSON for '+url+': '+e.message);}if(required&&!Array.isArray(parsed))throw new Error('Search index is not an array');return parsed;}
async function loadSearchAssets(){const index=await fetchJson('/search-index.json',true);let semanticIndex=null;try{semanticIndex=await fetchJson('/search-semantic-index.json',false);}catch(error){console.warn('Local semantic index unavailable; lexical search remains active.',error);}return {index,semanticIndex};}
loadSearchAssets().then(assets=>init(assets.index,assets.semanticIndex)).catch(function(err){init(fallbackIndex);if(count)count.textContent="Search index unavailable — showing verified fallback routes";if(answer)answer.textContent=["SEARCH HYBRID STATUS","> Fallback index active","> "+String(err.message||err).slice(0,120),"> Verified mission routes remain available"].join("\n");});
})();`;
const runtimePath=path.join(root,'search.js');fs.writeFileSync(runtimePath,runtime);
const syntax=spawnSync(process.execPath,['--check',runtimePath],{cwd:root,encoding:'utf8',stdio:'pipe'});
const required=['MatrixHybridSearch','search-semantic-index.json','Exact phrase and entity search','BM25 lexical retrieval','Compact local semantic vectors','investigationQueryPrefill',"cache:'no-store'",'HTML returned instead of JSON','fallbackIndex','const layerMap=','init(fallbackIndex)'];
const combined=html+'\n'+runtime;const missing=required.filter(marker=>!combined.includes(marker));const report={ok:syntax.status===0&&missing.length===0,generatedAt:new Date().toISOString(),engine:'matrix-hybrid-search-v1',missing,syntaxOk:syntax.status===0,syntaxError:syntax.status===0?null:String(syntax.stderr||syntax.stdout||'node --check failed'),boundary:'Semantic retrieval is local and additive. Exact/entity/BM25/domain/confidence controls remain active if the semantic asset is unavailable.'};
fs.writeFileSync(path.join(reports,'search-hybrid-install-report.json'),JSON.stringify(report,null,2)+'\n');if(!report.ok){console.error('SEARCH HYBRID INSTALL FAILED');missing.forEach(x=>console.error('- missing '+x));if(!report.syntaxOk)console.error(report.syntaxError);process.exit(1);}run('search-semantic-benchmark.js');console.log('Search Hybrid V1 installed: exact, entity, BM25, compact local semantic retrieval, domain filtering, reranking and confidence gating are active.');
