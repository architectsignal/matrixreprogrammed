name: Retired Repository-Credential One-Shot Deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Confirm the historical one-shot lane is retired
    runs-on: ubuntu-latest
    steps:
      - name: Use the canonical controlled production workflow
        run: |
          echo "This repository-credential push lane expired with its date-bound billing exception."
          echo "Run the manual Matrix Reprogrammed Controlled Production Release workflow in deploy.yml."
