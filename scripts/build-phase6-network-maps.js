const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'network-maps.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/network-maps.json found. Skipping Phase 6 Network Map build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
const books = fs.existsSync(path.join(root, 'data', 'books.json')) ? JSON.parse(fs.readFileSync(path.join(root, 'data', 'books.json'), 'utf8')).books.filter(b => b.status !== 'planned' && b.status !== 'unpublished') : [];
const atlas = fs.existsSync(path.join(root, 'data', 'power-atlas.json')) ? JSON.parse(fs.readFileSync(path.join(root, 'data', 'power-atlas.json'), 'utf8')) : { nodes: [] };
const vault = fs.existsSync(path.join(root, 'data', 'evidence-vault.json')) ? JSON.parse(fs.readFileSync(path.join(root, 'data', 'evidence-vault.json'), 'utf8')) : { sourceLanes: [] };
const answers = fs.existsSync(path.join(root, 'data', 'ai-answer-engine.json')) ? JSON.parse(fs.readFileSync(path.join(root, 'data', 'ai-answer-engine.json'), 'utf8')).answers || [] : [];

function esc(s=''){return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
function bookByKey(key){return books.find(b=>b.key===key);}
function bookUrl(b){return b && (b.generatedUrl || b.localUrl || 'books.html');}
function atlasNode(slug){return (atlas.nodes||[]).find(n=>n.slug===slug);}
function evidenceLane(slug){return (vault.sourceLanes||[]).find(l=>l.slug===slug);}
function answerNode(slug){return answers.find(a=>a.slug===slug);}
function nav(){return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="books.html">Books</a><a href="book-universe.html">Book Universe</a><a href="answer-engine.html">AI Answers</a><a href="power-atlas.html">Power Atlas</a><a href="network-maps.html">Network Maps</a><a href="network-map-index.html">Map Index</a><a href="evidence-vault.html">Evidence Vault</a><a href="news.html">Intel Desk</a><a href="forum.html">Signal Board</a><a href="search.html">Search</a><a href="black-file.html">Black File</a></nav></header>`;}
function layout({title,desc,body,schema=''}){return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="article" /><link rel="stylesheet" href="styles.css" />${schema}</head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Network Map boundary: a line shows a defined relationship only. Association is not guilt. A map is a navigation layer, not a verdict.</p></footer></div><script src="matrix.js"></script></body></html>`;}
function schema(map,file){return `<script type="application/ld+json">${JSON.stringify({'@context':'https://schema.org','@type':'Article',headline:map.title,description:map.summary,author:{'@type':'Person',name:'Nicholas Matthews'},publisher:{'@type':'Organization',name:'Matrix Reprogrammed'},url:`https://matrixreprogrammed.com/${file}`,dateModified:data.updated || '2026-06-24'})}</script>`;}
function atlasCards(slugs){return slugs.map(slug=>atlasNode(slug)).filter(Boolean).map(n=>`<article class="card redline"><span class="label">Power Atlas · ${esc(n.evidenceClass)}</span><h3>${esc(n.shortTitle||n.title)}</h3><p>${esc(n.summary)}</p><a class="btn" href="atlas-${esc(n.slug)}.html">Open Node</a></article>`).join('');}
function laneCards(slugs){return slugs.map(slug=>evidenceLane(slug)).filter(Boolean).map(l=>`<article class="card"><span class="label">Evidence Vault · ${esc(l.evidenceClass)}</span><h3>${esc(l.title)}</h3><p>${esc(l.summary)}</p><a class="btn alt" href="evidence-lane-${esc(l.slug)}.html">Open Lane</a></article>`).join('');}
function bookCards(keys){return keys.map(k=>bookByKey(k)).filter(Boolean).map(b=>`<article class="card"><span class="label">${esc(b.category)}</span><h3>${esc(b.title)}</h3><p>${esc(b.description)}</p><a class="btn" href="${esc(bookUrl(b))}">Open Book</a></article>`).join('');}
function answerCards(slugs){return slugs.map(slug=>answerNode(slug)).filter(Boolean).map(a=>`<article class="card"><span class="label">AI Answer</span><h3>${esc(a.question)}</h3><p>${esc(a.shortAnswer.slice(0,220))}...</p><a class="btn alt" href="answer-${esc(a.slug)}.html">Open Answer</a></article>`).join('');}
function relationshipRows(rows){return rows.map(r=>`<article class="card redline"><span class="label">${esc(r.type)} · ${esc(r.evidenceClass)}</span><h3>${esc(r.from)} → ${esc(r.to)}</h3><p>${esc(r.meaning)}</p></article>`).join('');}
function addPagesToSitemap(files){const p=path.join(root,'sitemap.xml');if(!fs.existsSync(p))return;let xml=fs.readFileSync(p,'utf8');const add=files.filter(f=>!xml.includes(`/${f}</loc>`)).map(f=>`  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated||'2026-06-24')}</lastmod><changefreq>weekly</changefreq><priority>0.9</priority></url>`).join('\n');if(add)xml=xml.replace('</urlset>',`${add}\n</urlset>`);fs.writeFileSync(p,xml);}
function patchLlms(files){const p=path.join(root,'llms.txt');if(!fs.existsSync(p))return;let llms=fs.readFileSync(p,'utf8');if(llms.includes('/network-map-index.html'))return;fs.writeFileSync(p,`${llms.trim()}\n\nPhase 6 Network Map Engine:\n- /network-map-index.html: generated network-map index connecting Power Atlas nodes, Evidence Vault lanes, books, and AI answers through labeled relationship lines.\n${files.slice(0,20).map(f=>`- /${f}: generated network map page with relationship-line discipline and archive routes.`).join('\n')}\n`);}
function patchSearchIndex(files){const p=path.join(root,'search-index.json');if(!fs.existsSync(p))return;const search=JSON.parse(fs.readFileSync(p,'utf8'));const existing=new Set(search.map(i=>i.url));for(const map of data.maps||[]){const url=`map-${map.slug}.html`;if(existing.has(url))continue;search.push({key:`network-map-${map.slug}`,title:`${map.title} | Network Map`,subtitle:map.subtitle,series:'Network Map Engine',category:map.primaryLayer,url,description:map.summary,keywords:[map.title,map.subtitle,map.summary,map.primaryLayer,...(map.atlasNodes||[]),...(map.evidenceLanes||[]),...(map.books||[]),...(map.answers||[])].filter(Boolean)});existing.add(url);}if(!existing.has('network-map-index.html'))search.push({key:'network-map-index',title:'Network Map Index | Matrix Reprogrammed',subtitle:'Phase 6 Network Map Engine',series:'Network Map Engine',category:'Power Map',url:'network-map-index.html',description:'Generated map index connecting Power Atlas nodes, Evidence Vault lanes, books, AI answers, and relationship-line rules.',keywords:['network maps','power map','relationship lines','Power Atlas','Evidence Vault']});fs.writeFileSync(p,JSON.stringify(search,null,2));}
function patchNetworkMapsHome(){const p=path.join(root,'network-maps.html');if(!fs.existsSync(p))return;let html=fs.readFileSync(p,'utf8');if(html.includes('id="phase-six-network-engine"'))return;const cards=(data.maps||[]).map(m=>`<article class="card redline"><span class="label">${esc(m.primaryLayer)}</span><h3>${esc(m.title)}</h3><p>${esc(m.summary)}</p><a class="btn" href="map-${esc(m.slug)}.html">Open Map</a></article>`).join('');const section=`<section id="phase-six-network-engine" class="section wrap"><h2>Phase 6 Network Map Engine</h2><p class="lead">Network maps now connect Atlas nodes, Evidence Vault lanes, books, AI answers, and relationship-line definitions without turning association into accusation.</p><div class="cta-row"><a class="btn" href="network-map-index.html">Open Map Index</a><a class="btn alt" href="power-atlas.html">Power Atlas</a><a class="btn alt" href="evidence-vault.html">Evidence Vault</a></div><div class="grid">${cards}</div></section>`;html=html.replace('</main>',`${section}</main>`);fs.writeFileSync(p,html);}

const generated=[];
for(const map of data.maps||[]){const file=`map-${map.slug}.html`;generated.push(file);const body=`<main><section class="hero wrap"><div class="eyebrow">Phase 6 Network Map</div><h1>${esc(map.title)}</h1><p class="lead">${esc(map.summary)}</p><div class="cta-row"><a class="btn" href="network-map-index.html">Map Index</a><a class="btn alt" href="network-maps.html">Network Maps</a><a class="btn alt" href="power-atlas.html">Power Atlas</a><a class="btn alt" href="evidence-vault.html">Evidence Vault</a></div></section><section class="section wrap split"><div class="terminal">MAP DISCIPLINE\n&gt; Primary layer: ${esc(map.primaryLayer)}\n&gt; Every line has a type\n&gt; Every line has an evidence class\n&gt; Association is not guilt\n&gt; Source lanes define what can be claimed\n&gt; Maps route deeper into books and answers</div><aside class="card redline"><h2>Risk Boundary</h2><p>${esc(map.riskBoundary)}</p></aside></section><section class="section wrap"><h2>Relationship Lines</h2><p class="lead">These are defined lines, not red-string insinuations.</p><div class="grid">${relationshipRows(map.relationships||[])}</div></section><section class="section wrap"><h2>Connected Power Atlas Nodes</h2><div class="grid">${atlasCards(map.atlasNodes||[])}</div><h2>Connected Evidence Vault Lanes</h2><div class="grid">${laneCards(map.evidenceLanes||[])}</div><h2>Book Routes</h2><div class="grid">${bookCards(map.books||[])}</div><h2>AI Answer Routes</h2><div class="grid">${answerCards(map.answers||[])}</div></section></main>`;fs.writeFileSync(path.join(root,file),layout({title:`${map.title} | Matrix Reprogrammed`,desc:map.summary,body,schema:schema(map,file)}));}
const mapCards=(data.maps||[]).map(m=>`<article class="card redline"><span class="label">${esc(m.primaryLayer)}</span><h3>${esc(m.title)}</h3><p>${esc(m.subtitle)}</p><p>${esc(m.summary)}</p><a class="btn" href="map-${esc(m.slug)}.html">Open Map</a></article>`).join('');
const rules=(data.lineRules||[]).map(rule=>`<article class="card"><span class="label">Line Rule</span><h3>${esc(rule)}</h3><p>This rule prevents the map from becoming unsupported red-string content.</p></article>`).join('');
const indexBody=`<main><section class="hero wrap"><div class="eyebrow">Phase 6 Network Map Engine</div><h1>NETWORK MAP INDEX.</h1><p class="lead">Relationship maps for intelligence, crime-state overlap, war machinery, elite access, symbolic power, and medical policy power.</p><div class="cta-row"><a class="btn" href="network-maps.html">Network Maps</a><a class="btn alt" href="power-atlas.html">Power Atlas</a><a class="btn alt" href="evidence-vault.html">Evidence Vault</a><a class="btn alt" href="answer-engine.html">AI Answers</a></div></section><section class="section wrap split"><div class="terminal">NETWORK MAP ENGINE STATUS\n&gt; Map lanes: ${(data.maps||[]).length}\n&gt; Line rules: ${(data.lineRules||[]).length}\n&gt; Relationship lines: ${(data.maps||[]).reduce((n,m)=>n+(m.relationships||[]).length,0)}\n&gt; Search index: expanded\n&gt; Sitemap: expanded\n&gt; llms.txt: expanded</div><aside class="card redline"><h2>Map Rule</h2><p>A line without a type is propaganda. A line with a source boundary becomes intelligence.</p></aside></section><section class="section wrap"><h2>Map Lanes</h2><div class="grid">${mapCards}</div></section><section class="section wrap"><h2>Relationship Rules</h2><div class="grid">${rules}</div></section></main>`;
fs.writeFileSync(path.join(root,'network-map-index.html'),layout({title:'Network Map Index | Matrix Reprogrammed',desc:'Generated network map index for public-record relationship lines and archive routes.',body:indexBody}));generated.push('network-map-index.html');
patchNetworkMapsHome();addPagesToSitemap(generated);patchLlms(generated);patchSearchIndex(generated);
console.log(`Built Phase 6 Network Map Engine with ${(data.maps||[]).length} maps and ${generated.length} generated pages.`);
