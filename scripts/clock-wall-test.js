const fs=require('fs');const path=require('path');const root=process.cwd();const f=p=>path.join(root,p);const failures=[];function exists(p){if(!fs.existsSync(f(p)))failures.push(p+' missing')}function text(p,m){if(!fs.existsSync(f(p))||!fs.readFileSync(f(p),'utf8').includes(m))failures.push(p+' missing '+m)}exists('timers.html');exists('data/clock-wall.json');exists('downloads/clock-wall.md');text('timers.html','RISK TIMERS.');text('timers.html','Connected systems');text('timers.html','control-brain.html');text('timers.html','elite-reports.html');text('timers.html','daily-missing-records.html');try{const data=JSON.parse(fs.readFileSync(f('data/clock-wall.json'),'utf8'));if(!data.ok)failures.push('clock wall json not ok');if(!Array.isArray(data.wall)||data.wall.length<1)failures.push('clock wall json has no clocks');for(const c of data.wall){if(!Array.isArray(c.feeds)||c.feeds.length<6)failures.push('clock missing connected feeds: '+c.title)}}catch{failures.push('clock wall json invalid')}if(failures.length){console.error('CLOCK WALL TEST FAILED');for(const x of failures)console.error('- '+x);process.exit(1)}console.log('CLOCK WALL TEST PASSED');