const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'content-distribution.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/content-distribution.json found. Skipping Phase 9 Content Distribution build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
function esc(s = '') { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
function nav() { return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="books.html">Books</a><a href="conversion-funnel.html">Funnels</a><a href="trust-center.html">Trust Center</a><a href="distribution-center.html">Distribution</a><a href="answer-engine.html">AI Answers</a><a href="power-atlas.html">Power Atlas</a><a href="network-map-index.html">Map Index</a><a href="evidence-vault.html">Evidence Vault</a><a href="news.html">Intel Desk</a><a href="forum.html">Signal Board</a><a href="search.html">Search</a><a href="black-file.html">Black File</a></nav></header>`; }
function layout(title, desc, body) { return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Distribution boundary: no clip, thread, or hook may outrun the evidence label behind it.</p></footer></div><script src="matrix.js"></script></body></html>`; }
function routeButtons(routes = []) { return routes.map(route => `<a class="btn alt" href="${esc(route)}">${esc(route.replace(/\.html$/, '').replace(/-/g, ' '))}</a>`).join(''); }
function templateCards(format) { return (format.template || []).map(step => `<article class="card"><span class="label">Template Step</span><h3>${esc(step)}</h3><p>Keep this step source-bound and route the audience deeper into the archive.</p></article>`).join(''); }
function briefCards(format) { return (format.sampleBriefs || []).map(brief => `<article class="card redline"><span class="label">Brief · ${esc(format.label)}</span><h3>${esc(brief.title)}</h3><p>${esc(brief.hook)}</p><a class="btn" href="${esc(brief.source)}">Open Source Route</a></article>`).join(''); }
function formatPage(format) {
  const file = `distribution-${format.slug}.html`;
  const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 9 Content Distribution</div><h1>${esc(format.title)}</h1><p class="lead">${esc(format.summary)}</p><div class="cta-row"><a class="btn" href="distribution-center.html">Distribution Center</a><a class="btn alt" href="trust-center.html">Trust Center</a><a class="btn alt" href="conversion-funnel.html">Funnels</a></div></section><section class="section wrap split"><div class="terminal">DISTRIBUTION FORMAT\n&gt; Format: ${esc(format.label)}\n&gt; Template steps: ${(format.template || []).length}\n&gt; Sample briefs: ${(format.sampleBriefs || []).length}\n&gt; Evidence boundary: active\n&gt; Archive routes: ${(format.routes || []).length}\n&gt; Trust route: active</div><aside class="card redline"><h2>Format Rule</h2><p>${esc(format.summary)}</p></aside></section><section class="section wrap"><h2>Template</h2><div class="grid">${templateCards(format)}</div></section><section class="section wrap"><h2>Sample Briefs</h2><div class="grid">${briefCards(format)}</div></section><section class="section wrap"><h2>Archive Routes</h2><div class="card"><div class="cta-row small">${routeButtons(format.routes)}</div></div></section></main>`;
  fs.writeFileSync(path.join(root, file), layout(`${format.title} | Matrix Reprogrammed`, format.summary, body));
  return file;
}
function patchFile(file, marker, section) { const p = path.join(root, file); if (!fs.existsSync(p)) return; let html = fs.readFileSync(p, 'utf8'); if (html.includes(marker)) return; html = html.replace('</main>', `${section}</main>`); fs.writeFileSync(p, html); }
function addPagesToSitemap(files) { const p = path.join(root, 'sitemap.xml'); if (!fs.existsSync(p)) return; let xml = fs.readFileSync(p, 'utf8'); const add = files.filter(f => !xml.includes(`/${f}</loc>`)).map(f => `  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated || '2026-06-24')}</lastmod><changefreq>weekly</changefreq><priority>0.89</priority></url>`).join('\n'); if (add) xml = xml.replace('</urlset>', `${add}\n</urlset>`); fs.writeFileSync(p, xml); }
function patchLlms(files) { const p = path.join(root, 'llms.txt'); if (!fs.existsSync(p)) return; let llms = fs.readFileSync(p, 'utf8'); if (llms.includes('/distribution-center.html')) return; fs.writeFileSync(p, `${llms.trim()}\n\nPhase 9 Content Distribution Engine:\n- /distribution-center.html: content distribution hub turning archive material into short-form, longform, podcast, article, newsletter, and social-thread formats.\n${files.filter(f => f !== 'distribution-center.html').map(f => `- /${f}: generated distribution format page with templates and sample briefs.`).join('\n')}\n`); }
function patchSearchIndex(files) { const p = path.join(root, 'search-index.json'); if (!fs.existsSync(p)) return; const search = JSON.parse(fs.readFileSync(p, 'utf8')); const existing = new Set(search.map(i => i.url)); for (const format of data.formats || []) { const url = `distribution-${format.slug}.html`; if (!existing.has(url)) { search.push({ key: `distribution-${format.slug}`, title: `${format.title} | Distribution`, subtitle: format.label, series: 'Content Distribution Engine', category: 'Content System', url, description: format.summary, keywords: [format.title, format.label, format.summary, ...(format.template || []), ...(format.routes || [])] }); existing.add(url); } } if (!existing.has('distribution-center.html')) search.push({ key: 'distribution-center', title: 'Distribution Center | Matrix Reprogrammed', subtitle: 'Phase 9 Content Distribution', series: 'Content Distribution Engine', category: 'Content System', url: 'distribution-center.html', description: 'Distribution hub for transforming Matrix Reprogrammed archive routes into videos, podcasts, articles, newsletters, and social threads.', keywords: ['content distribution','video scripts','podcast briefs','newsletter','social threads','short-form'] }); fs.writeFileSync(p, JSON.stringify(search, null, 2)); }
const generated = [];
for (const format of data.formats || []) generated.push(formatPage(format));
const cards = (data.formats || []).map(format => `<article class="card redline"><span class="label">${esc(format.label)}</span><h3>${esc(format.title)}</h3><p>${esc(format.summary)}</p><a class="btn" href="distribution-${esc(format.slug)}.html">Open Format</a></article>`).join('');
const rules = (data.rules || []).map(rule => `<article class="card"><span class="label">Distribution Rule</span><h3>${esc(rule)}</h3><p>This protects the archive from viral overclaiming.</p></article>`).join('');
const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 9 Content Distribution Engine</div><h1>DISTRIBUTION CENTER.</h1><p class="lead">Turn the Matrix Reprogrammed archive into videos, podcasts, articles, newsletters, and social threads without losing evidence discipline.</p><div class="cta-row"><a class="btn" href="distribution-shorts.html">Shorts</a><a class="btn alt" href="distribution-longform-video.html">Longform</a><a class="btn alt" href="distribution-podcast.html">Podcast</a><a class="btn alt" href="trust-center.html">Trust Center</a></div></section><section class="section wrap split"><div class="terminal">DISTRIBUTION ENGINE STATUS\n&gt; Formats: ${(data.formats || []).length}\n&gt; Rules: ${(data.rules || []).length}\n&gt; Briefs: ${(data.formats || []).reduce((n, f) => n + (f.sampleBriefs || []).length, 0)}\n&gt; Templates: ${(data.formats || []).reduce((n, f) => n + (f.template || []).length, 0)}\n&gt; Search index: expanded\n&gt; Sitemap: expanded\n&gt; llms.txt: expanded</div><aside class="card redline"><h2>Distribution Rule</h2><p>A viral clip is useful only if it sends the viewer back to the source route.</p></aside></section><section class="section wrap"><h2>Distribution Formats</h2><div class="grid">${cards}</div></section><section class="section wrap"><h2>Rules</h2><div class="grid">${rules}</div></section></main>`;
fs.writeFileSync(path.join(root, 'distribution-center.html'), layout('Distribution Center | Matrix Reprogrammed', 'Content distribution hub for Matrix Reprogrammed.', body));
generated.push('distribution-center.html');
const section = `<section id="phase-nine-distribution-engine" class="section wrap"><h2>Phase 9 Content Distribution Engine</h2><p class="lead">Turn this route into shorts, longform video, podcast, newsletter, article, and social-thread briefs while preserving the evidence boundary.</p><div class="cta-row"><a class="btn" href="distribution-center.html">Distribution Center</a><a class="btn alt" href="distribution-shorts.html">Shorts</a><a class="btn alt" href="distribution-longform-video.html">Longform</a></div></section>`;
patchFile('videos.html', 'id="phase-nine-distribution-engine"', section);
patchFile('podcast.html', 'id="phase-nine-distribution-engine"', section);
patchFile('news.html', 'id="phase-nine-distribution-engine"', section);
addPagesToSitemap(generated);
patchLlms(generated);
patchSearchIndex(generated);
console.log(`Built Phase 9 Content Distribution with ${(data.formats || []).length} formats and ${generated.length} generated pages.`);
