# Cloudflare Workers deployment configuration
# Production deploy path: Cloudflare Workers Builds runs `npm run build` then `npx wrangler deploy`.
# The build command creates the static site in _site; src/worker.js handles backend routes.

name = "matrixreprogrammed"
main = "src/worker.js"
compatibility_date = "2026-06-25"
keep_vars = true

[assets]
directory = "./_site"
binding = "ASSETS"
run_worker_first = true

# Public Signal Board storage.
# Wrangler/Cloudflare Builds can auto-provision this KV namespace when no id is supplied.
# If Cloudflare shows a generated namespace ID after deploy, we can pin it here later.
[[kv_namespaces]]
binding = "FORUM_POSTS"
