const fs = require('fs');
const path = require('path');

const root = process.cwd();
const dataPath = path.join(root, 'data', 'campaign-calendar.json');
if (!fs.existsSync(dataPath)) {
  console.log('No data/campaign-calendar.json found. Skipping Phase 17 Campaign Calendar build.');
  process.exit(0);
}
const data = JSON.parse(fs.readFileSync(dataPath, 'utf8'));
const outDir = path.join(root, 'downloads');
if (!fs.existsSync(outDir)) fs.mkdirSync(outDir, { recursive: true });
function esc(s = '') { return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }
function nav() { return `<header class="wrap topbar"><a class="brand" href="index.html"><img src="sigil.png" alt="Matrix Reprogrammed sigil" /> MATRIX REPROGRAMMED</a><nav class="nav"><a href="index.html">Home</a><a href="start-here.html">Start Here</a><a href="launch-room.html">Launch Room</a><a href="share-center.html">Share Center</a><a href="feed-center.html">Feed Center</a><a href="download-center.html">Download Center</a><a href="trust-center.html">Trust Center</a><a href="evidence-vault.html">Evidence Vault</a><a href="forum.html">Signal Board</a></nav></header>`; }
function layout(title, desc, body) { return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>${esc(title)}</title><meta name="description" content="${esc(desc)}" /><meta property="og:title" content="${esc(title)}" /><meta property="og:description" content="${esc(desc)}" /><meta property="og:type" content="website" /><link rel="stylesheet" href="styles.css" /><script type="application/ld+json">${JSON.stringify({'@context':'https://schema.org','@type':'Schedule',name:title,description:desc})}</script></head><body><canvas id="matrix"></canvas><div class="signal-face"></div><div class="veil"></div><div class="page">${nav()}${body}<footer class="footer wrap"><p><strong>MATRIX REPROGRAMMED</strong> — decode the illusion, follow the signal.</p><p class="warning">Campaign boundary: launch rooms are reusable public plans, not background automation or live posting promises.</p></footer></div><script src="matrix.js"></script></body></html>`; }
function titleFromFile(file) { return file.replace(/^downloads\//, '').replace(/\.html$|\.json$|\.md$/g, '').replace(/-/g, ' '); }
function buttons(routes = []) { return routes.map(route => `<a class="btn alt" href="${esc(route)}">${esc(titleFromFile(route))}</a>`).join(''); }
function cards(items = []) { return items.map(item => `<article class="card"><span class="label">Daily Push</span><h3>${esc(item)}</h3><p>Use this step with the campaign's trust, evidence, share, feed, and download routes.</p></article>`).join(''); }
function campaignJson(c) { return { updated: data.updated, title: c.title, label: c.label, summary: c.summary, boundary: 'Campaign calendars are reusable public planning assets. They are not promises of automated or live posting.', primaryRoute: c.primaryRoute, shareRoute: c.shareRoute, downloadRoute: c.downloadRoute, feedRoute: c.feedRoute, trustRoute: c.trustRoute, evidenceRoute: c.evidenceRoute, readerPath: c.readerPath, dailyPushes: c.dailyPushes }; }
function campaignMarkdown(c) { return `# ${c.title}\n\n${c.summary}\n\n## Boundary\n\nCampaign calendars are reusable public planning assets. They are not promises of automated or live posting.\n\n## Core Routes\n\n- Primary: ${c.primaryRoute}\n- Share kit: ${c.shareRoute}\n- Download pack: ${c.downloadRoute}\n- Feed: ${c.feedRoute}\n- Trust: ${c.trustRoute}\n- Evidence: ${c.evidenceRoute}\n- Reader path: ${c.readerPath}\n\n## 7-Day Campaign\n\n${(c.dailyPushes || []).map(x => `- ${x}`).join('\n')}\n`; }
function buildCampaign(c) {
  const htmlFile = `campaign-${c.slug}.html`;
  const jsonFile = `downloads/campaign-${c.slug}.json`;
  const mdFile = `downloads/campaign-${c.slug}.md`;
  fs.writeFileSync(path.join(root, jsonFile), JSON.stringify(campaignJson(c), null, 2));
  fs.writeFileSync(path.join(root, mdFile), campaignMarkdown(c));
  const routeList = [c.primaryRoute, c.shareRoute, c.downloadRoute, c.feedRoute, c.trustRoute, c.evidenceRoute, c.readerPath];
  const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 17 Campaign Calendar</div><h1>${esc(c.title)}</h1><p class="lead">${esc(c.summary)}</p><div class="cta-row"><a class="btn" href="${esc(jsonFile)}">JSON Plan</a><a class="btn alt" href="${esc(mdFile)}">Markdown Plan</a><a class="btn alt" href="launch-room.html">Launch Room</a></div></section><section class="section wrap split"><div class="terminal">CAMPAIGN ROOM\n&gt; Campaign: ${esc(c.title)}\n&gt; Label: ${esc(c.label)}\n&gt; Pushes: ${(c.dailyPushes || []).length}\n&gt; Share kit: ${esc(c.shareRoute)}\n&gt; Download pack: ${esc(c.downloadRoute)}\n&gt; Feed route: ${esc(c.feedRoute)}\n&gt; Evidence route: ${esc(c.evidenceRoute)}</div><aside class="card redline"><h2>Campaign Boundary</h2><p>Campaign rooms organize public launch pushes. They do not post automatically or replace source boundaries.</p></aside></section><section class="section wrap"><h2>Campaign Routes</h2><div class="card"><div class="cta-row small">${buttons(routeList)}</div></div></section><section class="section wrap"><h2>7-Day Push Plan</h2><div class="grid">${cards(c.dailyPushes)}</div></section><section class="section wrap"><h2>Downloads</h2><div class="card"><div class="cta-row"><a class="btn" href="${esc(jsonFile)}">JSON Campaign</a><a class="btn alt" href="${esc(mdFile)}">Markdown Campaign</a></div></div></section></main>`;
  fs.writeFileSync(path.join(root, htmlFile), layout(`${c.title} | Matrix Reprogrammed`, c.summary, body));
  return [htmlFile, jsonFile, mdFile];
}
function patchFile(file, marker, section) { const p = path.join(root, file); if (!fs.existsSync(p)) return; let html = fs.readFileSync(p, 'utf8'); if (html.includes(marker)) return; html = html.replace('</main>', `${section}</main>`); fs.writeFileSync(p, html); }
function addPagesToSitemap(files) { const p = path.join(root, 'sitemap.xml'); if (!fs.existsSync(p)) return; let xml = fs.readFileSync(p, 'utf8'); const add = files.filter(f => f.endsWith('.html') && !xml.includes(`/${f}</loc>`)).map(f => `  <url><loc>https://matrixreprogrammed.com/${f}</loc><lastmod>${esc(data.updated)}</lastmod><changefreq>weekly</changefreq><priority>0.87</priority></url>`).join('\n'); if (add) xml = xml.replace('</urlset>', `${add}\n</urlset>`); fs.writeFileSync(p, xml); }
function patchLlms(files) { const p = path.join(root, 'llms.txt'); if (!fs.existsSync(p)) return; let llms = fs.readFileSync(p, 'utf8'); if (llms.includes('/launch-room.html')) return; fs.writeFileSync(p, `${llms.trim()}\n\nPhase 17 Campaign Calendar / Launch Room Engine:\n- /launch-room.html: public launch room hub for weekly campaign plans, share kits, feeds, downloads, trust, and evidence routes.\n${files.map(f => `- /${f}: generated campaign route or downloadable campaign plan.`).join('\n')}\n`); }
function patchSearchIndex(files) { const p = path.join(root, 'search-index.json'); if (!fs.existsSync(p)) return; const search = JSON.parse(fs.readFileSync(p, 'utf8')); const existing = new Set(search.map(i => i.url)); for (const c of data.campaigns || []) { const url = `campaign-${c.slug}.html`; if (!existing.has(url)) { search.push({ key: `campaign-${c.slug}`, title: `${c.title} | Campaign`, subtitle: c.label, series: 'Campaign Calendar Engine', category: 'Launch Room', url, description: c.summary, keywords: [c.title, c.label, c.summary, c.primaryRoute, c.shareRoute, c.downloadRoute, c.feedRoute] }); existing.add(url); } } if (!existing.has('launch-room.html')) search.push({ key: 'launch-room', title: 'Launch Room | Matrix Reprogrammed', subtitle: 'Phase 17 Campaign Calendar Engine', series: 'Campaign Calendar Engine', category: 'Launch Room', url: 'launch-room.html', description: 'Campaign calendar hub for Matrix Reprogrammed launch rooms.', keywords: ['launch room','campaign calendar','weekly campaign','book launch','share kits','feeds','downloads'] }); fs.writeFileSync(p, JSON.stringify(search, null, 2)); }
const outputs = [];
for (const c of data.campaigns || []) outputs.push(...buildCampaign(c));
const campaignCards = (data.campaigns || []).map(c => `<article class="card redline"><span class="label">${esc(c.label)}</span><h3>${esc(c.title)}</h3><p>${esc(c.summary)}</p><a class="btn" href="campaign-${esc(c.slug)}.html">Open Campaign</a></article>`).join('');
const ruleCards = (data.rules || []).map(rule => `<article class="card"><span class="label">Campaign Rule</span><h3>${esc(rule)}</h3><p>Launch rooms route back to trust, evidence, share, feed, download, and reader-path pages.</p></article>`).join('');
const body = `<main><section class="hero wrap"><div class="eyebrow">Phase 17 Campaign Calendar / Launch Room Engine</div><h1>LAUNCH ROOM.</h1><p class="lead">Reusable weekly campaign rooms for books, archive pushes, share kits, feed routes, download packs, trust pages, and evidence lanes.</p><div class="cta-row"><a class="btn" href="campaign-black-file-launch.html">Black File Launch</a><a class="btn alt" href="campaign-intelligence-dossiers-push.html">Intelligence Push</a><a class="btn alt" href="campaign-trust-evidence-push.html">Trust Push</a></div></section><section class="section wrap split"><div class="terminal">LAUNCH ROOM STATUS\n&gt; Campaigns: ${(data.campaigns || []).length}\n&gt; Rules: ${(data.rules || []).length}\n&gt; JSON plans: ${(data.campaigns || []).length}\n&gt; Markdown plans: ${(data.campaigns || []).length}\n&gt; Trust routes: active\n&gt; Evidence routes: active</div><aside class="card redline"><h2>Launch Boundary</h2><p>This is a public campaign-planning layer. It does not post, schedule, or automate anything by itself.</p></aside></section><section class="section wrap"><h2>Campaign Rooms</h2><div class="grid">${campaignCards}</div></section><section class="section wrap"><h2>Campaign Rules</h2><div class="grid">${ruleCards}</div></section></main>`;
fs.writeFileSync(path.join(root, 'launch-room.html'), layout('Launch Room | Matrix Reprogrammed', 'Campaign calendar hub for Matrix Reprogrammed launch rooms.', body));
outputs.push('launch-room.html');
const section = `<section id="phase-seventeen-campaign-calendar-engine" class="section wrap"><h2>Phase 17 Campaign Calendar / Launch Room Engine</h2><p class="lead">This route now connects to weekly launch rooms, share kits, feeds, download packs, trust pages, and evidence lanes.</p><div class="cta-row"><a class="btn" href="launch-room.html">Launch Room</a><a class="btn alt" href="campaign-black-file-launch.html">Black File Campaign</a><a class="btn alt" href="campaign-trust-evidence-push.html">Trust Campaign</a></div></section>`;
for (const file of ['index.html','share-center.html','feed-center.html','download-center.html','sales-ladder.html','distribution-center.html']) patchFile(file, 'id="phase-seventeen-campaign-calendar-engine"', section);
addPagesToSitemap(outputs);
patchLlms(outputs);
patchSearchIndex(outputs);
console.log(`Built Phase 17 Campaign Calendar with ${(data.campaigns || []).length} campaigns and ${outputs.length} outputs.`);
