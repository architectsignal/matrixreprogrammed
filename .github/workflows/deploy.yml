name: Matrix Reprogrammed Production Deploy — HARD FREEZE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-freeze:
    name: Cloudflare production deployment is locked
    runs-on: ubuntu-latest
    steps:
      - name: Enforce production cost freeze
        run: |
          echo "PRODUCTION DEPLOYMENT LOCKED."
          echo "The single corrected Pyramid production run 30085279942 has already checked out the repaired combined release."
          echo "All later production runs fail closed. GitHub intelligence updates may continue without another Cloudflare deployment."